<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN"
"http://www.oasis-open.org/docbook/xml/4.1.2/docbookx.dtd">

 <chapter lang="es">
    <title>Kerberos</title>

	<sect1>
	<title>Introducci&#243;n</title>
		<para>Durante 1983 en el M.I.T. (<emphasis>Massachussetts Institute of Technology</emphasis>) comenzó el proyecto <emphasis>Athena</emphasis> con el objetivo de crear un entorno de trabajo educacional compuesto por estaciones gráficas, redes de alta velocidad y servidores; el sistema operativo para implementar este entorno era Unix 4.3BSD, y el sistema de autenticación utilizado en el proyecto se denominó <emphasis>Kerberos</emphasis> (<xref linkend="[MNSS87]" />) en honor al perro de tres cabezas que en la mitología griega vigila la puerta de entrada a Hades, el infierno.</para>

		<para>Hasta que se diseñó <emphasis>Kerberos</emphasis>, la autenticación en redes de computadores se realizaba principalmente de dos formas: o bien se aplicaba la autenticación por declaración (<emphasis>Authentication by assertion</emphasis>), en la que el usuario es libre de indicar el servicio al que desea acceder (por ejemplo, mediante el uso de un cliente determinado), o bien se utilizaban contraseñas para cada servicio de red. Evidentemente el primer modelo proporciona un nivel de seguridad muy bajo, ya que se le otorga demasiado poder al cliente sobre el servidor; el segundo modelo tampoco es muy bueno: por un lado se obliga al usuario a ir tecleando contínuamente su clave, de forma que se pierde demasiado tiempo y además la contraseña está viajando contínuamente por la red. <emphasis>Kerberos</emphasis> trata de mejorar estos esquemas intentando por un lado que un cliente necesite autorización para comunicar con un servidor (y que esa autorización provenga de una máquina confiable), y por otro eliminando la necesidad de demostrar el conocimiento de información privada (la contraseña del usuario) divulgando dicha información. </para>

		<para><emphasis>Kerberos</emphasis> se ha convertido desde entonces en un referente obligatorio a la hora de hablar de seguridad en redes. Se encuentra disponible para la mayoría de sistemas Unix, y viene integrado con OSF/DCE (<emphasis>Distributed Computing Environment</emphasis>). Está especialmente recomendado para sistemas operativos distribuidos, en los que la autenticación es una pieza fundamental para su funcionamiento: si conseguimos que un servidor logre conocer la identidad de un cliente puede decidir sobre la concesión de un servicio o la asignación de privilegios especiales. Sigue vigente en la actualidad (en su versión V a la hora de escribir este trabajo), a pesar del tiempo transcurrido desde su diseño; además fué el pionero de los sistemas de autenticación para sistemas en red, y muchos otros diseñados posteriormente, como KryptoKnight (xref linkend="[MTHZ92]", xref linkend="[JTY97]"...), SESAME ([xref linkend="PPK93"]) o Charon ([xref linkend="Atk93"]) se basan en mayor o menor medida en <emphasis>Kerberos</emphasis></para>

		<para>El uso de <emphasis>Kerberos</emphasis> se produce principalmente en el <emphasis>login</emphasis>, en el acceso a otros servidores (por ejemplo, mediante <screen>rlogin</screen>) y en el acceso a sistemas de ficheros en red como NFS. Una vez que un cliente está autenticado o bien se asume que todos sus mensajes son fiables, o si se desea mayor seguridad se puede elegir trabajar con mensajes seguros (autenticados) o privados (autenticados y cifrados). <emphasis>Kerberos</emphasis> se puede implementar en un servidor que se ejecute en una máquina segura, mediante un conjunto de bibliotecas que utilizan tanto los clientes como las aplicaciones; se trata de un sistema fácilmente escalable y que admite replicación, por lo que se puede utilizar incluso en sistemas de alta disponibilidad (aunque como veremos al final del capítulo está fuertemente centralizado).</para>
	
	</sect1>
	<sect1>
	<title>Arquitectura de Kerberos</title>
		<para>Un servidor <emphasis>Kerberos</emphasis> se denomina KDC (<emphasis>Kerberos Distribution Center</emphasis>), y provee de dos servicios fundamentales: el de autenticación (AS, <emphasis>Authentication Service</emphasis>) y el de <emphasis>tickets</emphasis> (TGS, <emphasis>Ticket Granting Service</emphasis>). El primero tiene como función autenticar inicialmente a los clientes y proporcionarles un <emphasis>ticket</emphasis> para comunicarse con el segundo, el servidor de tickets, que proporcionará a los clientes las credenciales necesarias para comunicarse con un servidor final que es quien realmente ofrece un servicio. Además, el servidor posee una base de datos de sus clientes (usuarios o programas) con sus respectivas claves privadas, conocidas únicamente por dicho servidor y por el cliente que al que pertenece.</para>

		<para>La arquitectura de <emphasis>Kerberos</emphasis> está basada en tres objetos de seguridad: Clave de Sesión, <emphasis>Ticket</emphasis> y Autenticador.</para>

		<itemizedlist mark='Troff'>
		<listitem>
			<para>La clave de sesión es una clave secreta generada por <emphasis>Kerberos</emphasis> y expedida a un cliente para uso con un servidor durante una sesión; no es obligatorio utilizarla en toda la comunicación con el servidor, sólo si el servidor lo requiere (porque los datos son confidenciales) o si el servidor es un servidor de autenticación. Se suele denominar a esta clave , para la comunicación entre un cliente C y un servidor S.
Las claves de sesión se utilizan para minimizar el uso de las claves secretas de los diferentes agentes: éstas últimas son válidas durante mucho tiempo, por lo que es conveniente para minimizar ataques utilizarlas lo menos posible.</para>
		</listitem>
		<listitem>
			<para>El ticket es un testigo expedido a un cliente del servicio de <emphasis>ticket</emphasis>s de <emphasis>Kerberos</emphasis> para solicitar los servicios de un servidor; garantiza que el cliente ha sido autenticado recientemente. A un <emphasis>ticket</emphasis> de un cliente C para acceder a un servicio S se le denomina . Este <emphasis>ticket</emphasis> incluye el nombre del cliente C, para evitar su posible uso por impostores, un periodo de validez y una clave de sesión asociada para uso de cliente y servidor. <emphasis>Kerberos</emphasis> siempre proporciona el <emphasis>ticket</emphasis> ya cifrado con la clave secreta del servidor al que se le entrega.</para>
		</listitem>
		<listitem>
			<para>El autenticador es un testigo construido por el cliente y enviado a un servidor para probar su identidad y la actualidad de la comunicación; sólo puede ser utilizado una vez. Un autenticador de un cliente C ante un servidor S se denota por . Este autenticador contiene, cifrado con la clave de la sesión, el nombre del cliente y un <emphasis>timestamp</emphasis>.</para>
</listitem>
</itemizedlist>	


</sect1>
		

 </chapter>