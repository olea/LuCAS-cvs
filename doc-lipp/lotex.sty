%%%
%%% Style file for the LoTeX tool.
%%% Currently, this simply defines the \iftex command and sets the
%%% corresponding flag to true. This allows to protect LoTeX-specific
%%% commands within a document from LaTeX:
%%% \iftex...\else ... here comes the LoTeX stuff ... \fi
\newif\iftex\textrue
\newif\iflotex\lotexfalse
\newif\ifinfo\infofalse
\newif\ifroff\rofffalse
\newif\ifmm\mmfalse

%%% Escape TeXinfo specials
\def\node#1#2#3{}
\def\makemenu{}
