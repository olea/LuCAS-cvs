<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- <!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN"
   "http://www.oasis-open.org/docbook/xml/4.0/docbookx.dtd">
   -->
   
<article lang="es" id="frames">
  <articleinfo>
    <title>Frames, las ventanas llegan al WWW</title>
    <date>7 de Abril de 2002</date>
    <author>
      <firstname>Jorge</firstname>
      <surname>Ferrer</surname>
    </author>
    <author>
      <firstname>Víctor</firstname>
      <surname>García</surname>
    </author>
    <abstract>
      <para>
	Una de las extensiones usadas más habitualmente el WWW son los
	<emphasis>marcos</emphasis> o <emphasis>frames</emphasis>. A
	algunos programadores les horroriza la idea de que sus 
	páginas tengan frames, otros no entienden como han podido vivir sin
	ellos hasta ahora. Pero, lo que está claro es que existen
	páginas web donde se ha demostrado que al menos en ciertos
	casos un buen uso de frames mejora mucho la experiencia del
	navegante. 
      </para>
      <para>
	En este capítulo se explica cómo usarlos y usarlos bien. Entre
	las características avanzadas se verá cómo anidar
	framesets para crear estructuras complejas, cómo manejar y
	ocultar los bordes y cómo decidir en qué frame debe mostrarse
	el resultado de un enlace. Después pasaremos a explicar la
	quinta práctica del curso, en la que añadiremos frames a la
	página web de Los Alpes. Como guinda, aprenderemos a usar
	frames flotantes, un recurso que forma parte del estándar HTML
	4.0 pero que no es muy usado aún.
      </para>
    </abstract>

    <editor>
      <firstname>Jorge</firstname>
      <surname>Ferrer</surname>
    </editor>
    <releaseinfo>Versión 1.0</releaseinfo>
    <orgname>Conquista la web</orgname>
    <legalnotice>
      <para>
	Copyright Jorge Ferrer y Victor García. Se otorga permiso para copiar,
	distribuir y/o modificar este documento bajo los términos de la
	Licencia de Documentación Libre GNU, Versión 1.1 o cualquier otra
	versión posterior publicada por la Free Software Foundation. Puede
	consultar una copia de la licencia en:
	http://www.gnu.org/copyleft/fdl.html
      </para>
    </legalnotice>
  </articleinfo>

  <para>
    Los frames consisten en una técnica para insertar varios documentos
    HTML en una sola ventana del navegador. Para ello divide la
    pantalla en varias zonas o secciones que reciben el nombre de
    <emphasis>frames</emphasis> o <emphasis>marcos</emphasis>. En cada
    uno de estos marcos debemos insertar un contenido, que en general
    constará de un documento HTML, aunque también puede ser una imagen
    o algún otro elemento del WWW visualizable por los navegadores. En
    la figura 8.1 podemos ver un ejemplo de <productname>Explorer
    </productname> dividido en 4 frames:
  </para>

  <figure label="8.1">

    <title>
      Los frames nos permiten dividir la ventana del navegador en
      varias secciones diferentes. En este caso la ventana a sido
      dividida en 4 frames.
    </title>
    <graphic fileref="img/fig8-01.png"/>

  </figure>

  <para>
    Una de las ventajas de los frames es que cada uno de los
    documentos HTML puede ser cambiado sin que ello afecte al resto.
    Ya en el capítulo anterior (figuras 9.1 y 9.6) vimos algunos
    ejemplos de uso de frames que nos pueden ayudar a hacernos una
    idea de su funcionamiento y de los que seremos capaces de hacer
    tras este capítulo.
  </para>

  <para>
    Además de sus ventajas los frames también tienen algunos
    inconvenientes. Por ello desde su aparición en la versión 1.0 del
    navegador <productname>Netscape Navigator</productname> han sido el
    foco de una gran controversia. Uno de los principales problemas de
    los frames es el hecho de ser una extensión del lenguaje HTML y no
    formar parte del estándar. Por esta razón no todos los navegadores
    están preparados para ellos y no podrán visualizar páginas que
    contengan frames. Pero afortunadamente, hoy en día la mayor parte
    de los navegadores gráficos los soportan. Entre estos se incluyen
    <productname>Netscape Navigator 1.0</productname> o superior (por
    supuesto), <productname>Microsoft Internet Explorer 3.0
    </productname> o superior y <productname>Opera 3.0</productname>.
    Además existen métodos para solucionar el problema de los
    navegadores no compatibles.
  </para>

  <note>

    <para>
      Actualmente todos los navegadores gráficos habitualmente usados
      desde ordenadores personales soportan el uso de frames. Sin
      embargo en navegadores para dispositivos móviles, navegadores
      textuales y navegadores que leen la página el uso de frames
      puede ser perjudicial.
    </para>

  </note>

  <para>
    El segundo gran foco de la controversia es mucho menos técnico y
    radica en la discusión de si los frames son útiles o no.
    Probablemente el origen de esta discusión surgió con el BOOM
    sucedido cuando aparecieron los frames. Debido a lo novedoso del
    tema, una gran parte de los programadores de páginas Web empezaron
    a usar frames indiscriminadamente, con lo que el WWW se pobló de
    ellos y en la mayoría de los casos estos sólo eran usados porque
    eran algo nuevo, pero perjudicaban la navegación. Por esta razón
    empezaron a surgir detractores de los frames. Pero, ahora, pasado
    un tiempo ya no existen tantos sitios Webs que usan frames de
    manera abusiva, con lo que aquellos que discutían empiezan a llegar
    a un acuerdo. Lo que casi todo el mundo parece admitir es que en
    ciertas ocasiones, como la que nos encontramos en el Web de la
    figura 8.2, el uso de frames puede ser conveniente y ayuda a la
    navegación, que es en definitiva el motivo por el que surgieron en
    primer lugar.
  </para>

  <figure label="8.2">

    <title>
      En el Web <ulink url="http://www.chess.com">www.chess.com</ulink>
      podemos ver un excelente ejemplo del uso de frames para mantener
      visibles las barras de navegación.
    </title>
    <graphic fileref="img/fig8-02.png"/>

  </figure>

  <para>
    En este Web existen tres frames. A la izquierda se ha usado uno
    como barra de navegación donde se encuentran enlaces a las
    distintas secciones. En la parte superior nos encontramos con un
    segundo frame que contiene el encabezado gráfico de la página con
    el logo de los <emphasis>Aficionado</emphasis> así como otros
    links. El tercero de los frames es el que ocupa una mayor área, y
    en el se encuentra el contenido de la página Web. El objetivo de
    los frames en este caso consiste en mantener siempre visible el
    encabezado y barra de navegación mientras el visitante se mueve por
    el contenido principal usando las barras de desplazamiento, las
    cuales, como podemos observar, sólo afectan al tercero de los
    frames. En definitiva los frames son, sin duda, útiles para
    mantener visibles barras de navegación (gráficas o simplemente
    enlaces hipertexto) o encabezados y/o logotipos muy importantes
    aunque el navegante se desplace por el contenido de la página o
    navegue por el sitio Web.
  </para>

  <para>
    Sin perder más tiempo vamos a empezar ya a estudiar las etiquetas y
    atributos necesarios para crear los frames. Para empezar vamos a
    ver un atributo, TARGET, y una etiqueta, BASE, que están
    enormemente relacionados con la creación de páginas con frames.
    Posteriormente nos adentraremos en las etiquetas necesarias para
    crear la estructura de frames en las páginas.
  </para>

  <sect1>

    <title>El atributo TARGET</title>

    <para>
      El atributo TARGET es, al igual que los frames, una extensión
      introducida por <productname>Netscape</productname> y adoptada
      posteriormente por <productname>Explorer</productname> y <productname>
	Opera</productname>. El principal uso de este atributo es cuando se
      usa en la etiqueta A para crear enlaces y su función es indicar el
      objetivo (TARGET significa diana u objetivo) en el que se debe cargar
      la página a la que saltamos tras seleccionar el texto o imagen de ese
      enlace. La forma de usar este atributo es:
    </para>

    <programlisting>

      <sgmltag>
	&lt;A HREF="URL" TARGET="nombre_ventana"&gt;
      </sgmltag>
      ...
      <sgmltag>&lt;/A&gt;</sgmltag>

    </programlisting>

    <para>
      Aunque este atributo fue creado para los frames y es en ellos donde
      adquiere su verdadera utilidad también puede ser usado aunque estos
      no existan. Cuando el navegante selecciona un enlace que usa el
      atributo TARGET busca una ventana o frame (posteriormente veremos
      como se crean y cómo se les da nombre) cuyo nombre coincida con el
      que hemos indicado como valor de TARGET. Si existe, muestra la página
      a la que nos conduce el enlace en esa ventana o frame, pero si no
      existe abre una nueva ventana del navegador y la da el nombre
      indicado. En la figura 8.3 vemos una página con dos frames:
    </para>

    <figure label="8.3">

      <title>
	Usando el atributo TARGET podremos lograr que pulsando el enlace de
	un frame el resultado se muestre en otro.
      </title>
      <graphic fileref="img/fig8-03.png"/>

    </figure>

    <para>
      En la de la derecha hemos incluido un enlace usando como destino
      (TARGET) el frame de al lado. La flecha nos indica que al pulsar
      sobre el enlace aparece una nueva página en el frame de al lado.
    </para>

    <para>
      Posteriormente tendremos tiempo para practicar con frames, ahora
      vamos a ver un ejemplo del uso de este atributo en una página sin
      ellos, como las que hemos visto hasta ahora. Abrimos nuestro editor
      de texto o HTML favorito y creamos un archivo nuevo al que llamaremos
      <filename>madre.htm</filename> con este código:
    </para>

    <programlisting>

      <sgmltag>
	&lt;!DOCTYPE HTML
	PUBLIC "-//W3C//DTD HTML 3.2 Final//EN"&gt;
      </sgmltag>

      <sgmltag>&lt;HTML&gt;</sgmltag>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      <sgmltag>&lt;TITLE&gt;</sgmltag>
      Ventana Madre
      <sgmltag>&lt;/TITLE&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

      <sgmltag>&lt;BODY BGCOLOR="#EDB301"&gt;</sgmltag>
      <sgmltag>&lt;H1&gt;</sgmltag>
      Esta es la ventana Madre que generará el resto
      <sgmltag>&lt;/H1&gt;</sgmltag>
      <sgmltag>&lt;P&gt;</sgmltag>
      <sgmltag>
	&lt;A HREF="ventana1.htm"
	TARGET="primera_ventana"&gt;</sgmltag>
      Abrir
      <sgmltag>&lt;/A&gt;</sgmltag>
      una nueva ventana llamada primera_ventana
      <sgmltag>&lt;p&gt;</sgmltag>
      <sgmltag>
	&lt;A HREF="ventana2.htm"
	TARGET="segunda_ventana"&gt;</sgmltag>
      Abrir
      <sgmltag>&lt;/A&gt;</sgmltag>
      una nueva ventana llamada segunda_ventana
      <sgmltag>&lt;P&gt;</sgmltag>
      <sgmltag>
	&lt;A HREF="ventana3.htm"
	TARGET="primera_ventana"&gt;</sgmltag>
      Poner
      <sgmltag>&lt;/A&gt;</sgmltag>
      contenido <sgmltag>&lt;STRONG&gt;</sgmltag>nuevo
      <sgmltag>&lt;/STRONG&gt;</sgmltag> en la primera_ventana
      <sgmltag>&lt;/BODY&gt;</sgmltag>
      <sgmltag>&lt;/HTML&gt;</sgmltag>

    </programlisting>

    <para>
      Posteriormente debemos crear los archivos
      <filename>ventana1.htm</filename>,
      <filename>ventana2.htm </filename> y <filename>ventana3.htm</filename>, que tendrán un código muy simple en el que simplemente
      debemos poner un texto que diga que ventana es esa. Por ejemplo en el
      caso de <filename>ventana1.htm</filename> podemos
      escribir el siguiente código.
    </para>

    <programlisting>

      <sgmltag>
	&lt;!DOCTYPE HTML
	PUBLIC "-//W3C//DTD HTML 3.2 Final//EN"&gt;
      </sgmltag>

      <sgmltag>&lt;HTML&gt;</sgmltag>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      <sgmltag>&lt;TITLE&gt;</sgmltag>
      Página 1
      <sgmltag>&lt;/TITLE&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

      <sgmltag>&lt;BODY&gt;</sgmltag>
      <sgmltag>&lt;H1&gt;</sgmltag>
      Esta es la página 1
      <sgmltag>&lt;/H1&gt;</sgmltag>
      Esta página debe mostrarse en la ventana
      <sgmltag>&lt;STRONG&gt;</sgmltag>primera_ventana
      <sgmltag>&lt;/STRONG&gt;</sgmltag>
      <sgmltag>&lt;/BODY&gt;</sgmltag>

      <sgmltag>&lt;/HTML&gt;</sgmltag>

    </programlisting>

    <para>
      Creamos el resto de archivos con un código similar a este y abrimos
      el archivo <filename>madre.htm</filename> con el
      navegador que prefiramos. A continuación seguimos los siguientes
      pasos:
    </para>

    <orderedlist>

      <listitem>

	<para>
	  Pulsamos sobre el primero de los enlaces. Observaremos que se
	  crea una nueva ventana con el contenido de <filename>
	    pagina1.htm</filename>.
	</para>

      </listitem>

      <listitem>

	<para>
	  Pulsamos sobre el segundo enlace. Se vuelve a crear una nueva
	  ventana, esta vez con el contenido de <filename>
	    pagina2.htm</filename>.
	</para>

      </listitem>

      <listitem>

	<para>
	  Pulsamos sobre la palabra &apos;Poner&apos; para activar el
	  tercero de los enlaces. En este caso el navegador busca una
	  ventana de nombre <emphasis>primera_ventana</emphasis> y la
	  encuentra, porque fue creada en el paso 1. Una vez encontrada
	  muestra en ella el contenido de
	  <filename>pagina3.htm</filename> quitando el contenido de
	  <filename>pagina1.htm</filename>. 
	</para>

      </listitem>

    </orderedlist>

    <para>
      Tras llevar a cabo estos pasos deberíamos obtener el resultado de la
      figura 8.4, en la que vemos la ventana madre y las otras dos
      abiertas:
    </para>

    <figure label="8.4">

      <title>
	Este debe ser el resultado final tras llevar a cabo todos los pasos
	en el ejemplo de uso del atributo TARGET para crear varias
	ventanas.
      </title>
      <graphic fileref="img/fig8-04.png"/>

    </figure>

    <para>
      Hemos puesto un color de fondo a <filename>ventana3.htm</filename>para que resalte más.
    </para>

  </sect1>

  <sect1>

    <title>La etiqueta BASE</title>

    <para>
      Cuando en el capítulo 2 aprendimos a crear enlaces vimos que había
      dos formas de especificar la dirección de destino. La primera era
      poniendo la dirección relativa. Por ejemplo, desde un archivo llamado
      <filename>index.htm</filename> situado en
      <ulink url="http://freek.futurnet.es">freek.futurnet.es</ulink>
      podríamos enlazar una página llamada <filename>pag.htm
      </filename>y situada en el directorio <emphasis>varios
      </emphasis>usando el código:
    </para>

    <programlisting>

      <sgmltag>&lt;A HREF="varios/pag.htm"&gt;</sgmltag>
      ...
      <sgmltag>&lt;/A&gt;</sgmltag>

    </programlisting>

    <para>
      Una segunda forma para enlazar esta misma página era poniendo la
      dirección completa (URL) de Internet, siguiendo con el ejemplo
      anterior debemos usar el siguiente código:
    </para>

    <programlisting>

      <sgmltag>
	&lt;A HREF="http://freek.futurnet.es/varios/pag.htm"&gt;
      </sgmltag>
      ...
      <sgmltag>&lt;/A&gt;</sgmltag>

    </programlisting>

    <para>
      Comparando los dos ejemplos anteriores podemos deducir que cuando
      ponemos una dirección relativa el navegador le añade la dirección
      completa (exceptuando el nombre del archivo) de la página actual. Es
      decir, al usar en este caso la dirección relativa, el navegador ve
      que la página actual está en <ulink url="http://freek.futurnet.es">
	freek.futurnet.es</ulink> y se lo añade a la dirección relativa para
      obtener una dirección absoluta. Esta dirección que es añadida a las
      direcciones relativas para obtener la dirección completa recibe el
      nombre de <emphasis>dirección base</emphasis>, y en todos los casos
      que hemos visto hasta ahora la dirección base coincidía con la
      dirección de la página actual.
    </para>

    <para>
      Pues bien, la etiqueta <emphasis>BASE</emphasis> nos permitirá
      cambiar esa dirección base, y ahora podremos elegir cualquier
      dirección del WWW como base para todos los enlaces con direcciones
      relativas de la página. Esta etiqueta debe ser usada en la cabecera
      del documento, entre <sgmltag>&lt;HEAD&gt;</sgmltag> y
      <sgmltag>&lt;/HEAD&gt;</sgmltag>, dado que afectará a todo este. Para
      especificar la dirección se usará un atributo llamado <emphasis>HREF
      </emphasis>. Veamos un ejemplo, si queremos poner como dirección base
      de un documento HTML la dirección
      <ulink url="http://freek.jorgeferrer.com">freek.jorgeferrer.com
      </ulink> debemos escribir:
    </para>

    <programlisting>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      ...Más código...
      <sgmltag>
	&lt;BASE HREF="http://freek.jorgeferrer.com/"&gt;
      </sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

    </programlisting>

    <para>
      O bien:
    </para>

    <programlisting>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      ...Más código...
      <sgmltag>&lt;
	BASE HREF="http://freek.jorgeferrer.com/index.htm"
	&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

    </programlisting>

    <para>
      En la figura 8.5 podemos ver la diferencia entre usar y no usar BASE
      y como afecta ello al significado de una dirección relativa.
    </para>

    <figure label="8.5">

      <title>
	Variando la dirección de base con la etiqueta BASE variará el
	significado de las direcciones relativas.
      </title>
      <graphic fileref="img/fig8-05.png"/>

    </figure>

    <warning>

      <para>
	Las direcciones que pongamos como valor del atributo HREF de la
	etiqueta BASE deben ser siempre absolutas (completas).
      </para>

    </warning>

    <para>
      Es importante resaltar que aunque en todos los ejemplos que hemos
      puesto hasta ahora sólo hemos considerado los enlaces hipertexto la
      dirección base afecta a todas aquellas etiquetas en las que debemos
      especificar una dirección y podemos hacerlo de forma relativa, como
      por ejemplo en las imágenes. Por ejemplo si usamos BASE como hemos
      indicado en el código anterior y posteriormente insertamos una imagen
      con el código:
    </para>

    <programlisting>

      <sgmltag>&lt;IMG SRC="imagen.gif"&gt;</sgmltag>

    </programlisting>

    <para>
      Esta imagen debe estar en
      <ulink url="http://freek.jorgeferrer.com/imagen.gif">
	freek.jorgeferrer.com/imagen.gif</ulink> y no en el mismo directorio
      que la página actual como sucedía hasta ahora cuando no usábamos la
      etiqueta BASE.
    </para>

    <note>

      <para>
	La etiqueta BASE existe desde la primera versión del estándar
	HTML: el HTML 2.0. Sin embargo hemos preferido no comentarla hasta
	ahora porque, como enseguida veremos, es con los frames donde
	realmente su uso es importante, casi imprescindible.
      </para>

    </note>

    <para>
      La pregunta que quizá nos hagamos ahora es ¿para qué puede servirnos
      cambiar la dirección base?. Puede haber diversas causas, por ejemplo
      existe la posibilidad de que tengamos nuestra página principal en un
      servidor, pero el resto de las páginas y enlaces en otro o
      simplemente que estén todas en otro directorio. En ambos casos
      podemos ahorrarnos poner el otro servidor o directorio
      especificándolo como dirección base.
    </para>

    <para>
      Sin duda esta forma de usar la etiqueta BASE puede llegar a ser útil,
      pero cuando se convierte en imprescindible es al usar un segundo
      atributo: <emphasis>TARGET</emphasis>. Hemos visto un poco antes que
      cuando trabajamos con páginas con varios frames se puede incluir el
      atributo TARGET en los enlaces para indicar el frame en el que debe
      mostrarse la página enlazada. Pues bien usando este mismo atributo en
      la etiqueta BASE lo que conseguimos es establecer el frame base donde
      se mostrará la página enlazada cuando pulsemos uno de los enlaces del
      frame actual. ¿Porqué es esto tan útil? Lo entenderemos un poco mejor
      en cuanto empecemos a trabajar con frames, pero podemos adelantar un
      caso en el que su utilidad resulta obvia. En la figura 8.6 vemos un
      ejemplo simple pero que ilustra lo que queremos a la perfección:
    </para>

    <figure label="8.6">

      <title>
	En este Web pulsando sobre los enlaces del frame izquierdo nos
	movemos por los diferentes temas del frame derecho.
      </title>
      <graphic fileref="img/fig8-06.png"/>

    </figure>

    <para>
      La página contiene una serie de enlaces organizados por temas y está
      dividida en dos frames. En el frame de la izquierda hay un listado de
      los diferentes temas y en el de la derecha los enlaces en sí. Pulsando
      sobre los temas del frame izquierdo se muestran los enlaces de ese
      tema en el frame derecho. En este caso es claro que todas las páginas
      a las que apuntan los enlaces deben ser mostradas en el frame derecho
      y por tanto lo más cómodo (y así es como está hecho en realidad) es
      usar (suponiendo que el nombre del frame derecho es <emphasis>derecho
      </emphasis>):
    </para>

    <programlisting>

      <sgmltag>&lt;BASE TARGET="derecho"&gt;</sgmltag>

    </programlisting>

    <para>
      En el código del frame de la izquierda, con lo que nos ahorramos usar
      el atributo TARGET en cada uno de sus enlaces.
    </para>

    <para>
      Ya hemos aprendido todo lo necesario para comenzar con la
      construcción de páginas con frames, así que sin perder más tiempo
      pasamos ya a estudiar las etiquetas del lenguaje HTML que debemos
      usar.
    </para>

  </sect1>

  <sect1>

    <title>Creación de páginas con frames</title>

    <para>
      El hecho de usar frames no sólo cambia por completo la apariencia de
      nuestra página, sino que también cambia el proceso de su
      realización. Si hasta ahora los Webs estaban formados por un único
      documento HTML donde estaba el contenido, al usar frames será
      necesario un documento HTML para definir la estructura de la página
      (número de frames a usar y disposición de estos), y posteriormente
      tantos documentos HTML extra como frames tengamos para insertar su
      contenido. En la figura 8.7 vemos un ejemplo con una página Web
      compuesta por 3 frames:
    </para>

    <figure label="8.7">

      <title>
	Los Webs con frames constarán de varios documentos HTML. En este
	ejemplo al haber tres frames serán necesarios 4 documentos HTML.
      </title>
      <graphic fileref="img/fig8-07.png"/>

    </figure>

    <para>
      En este caso serán necesarios 4 documentos HTML. El primero
      simplemente contiene el código que le indica al navegador el número
      de frames de la página, su tamaño, su posición y el nombre de los
      archivos donde está el contenido de los 3 frames, a este documento
      HTML le llamaremos <emphasis>documento de definición de frames
      </emphasis>. Los otros 3 documentos HTML tienen el contenido de cada
      uno de los tres frames y deben ser creados de la misma manera que
      hemos venido creando los documentos HTML hasta ahora, eso sí, hay que
      tener en cuenta que ese contenido será mostrado en un espacio
      reducido (el del frame correspondiente) y no en toda la ventana del
      navegador.
    </para>

  </sect1>

  <sect1>

    <title>Creación del documento de definición de frames</title>

    <para>
      La estructura del documento de definición de frames es parecida a la
      estructura de las páginas creadas hasta ahora. La principal
      diferencia será que este documento no tendrá contenido y por tanto no
      debe usarse la etiqueta BODY. A cambio ha de usarse una nueva
      etiqueta: <emphasis>FRAMESET</emphasis>. Esta será la etiqueta clave
      en la creación de los frames. La estructura básica del documento de
      definición será por tanto la que observamos en la figura 8.8:
    </para>

    <figure label="8.8">

      <title>
	La estructura de las páginas con frames es diferente a las que no
	tienen. La principal diferencia es la sustitución de BODY por
	FRAMESET.
      </title>
      <graphic fileref="img/fig8-08.png"/>

    </figure>

    <para>
      En ella se observa claramente como la etiqueta FRAMESET sustituye a
      BODY. Se podría decir que estas dos etiquetas son exclusivas, cuando
      esté una no puede estar la otra.
    </para>

    <para>
      El siguiente paso en la creación de nuestra página con frames será
      decidir cuantos queremos crear como serán. Aunque posteriormente
      veremos cómo se pueden crear divisiones tan complejas como queramos,
      por ahora crearemos páginas en las que la ventana del navegador está
      dividida en filas o en columnas.
    </para>

    <sect2>

      <title>División en columnas</title>

      <para>
	Para crear una página Web formada por varios frames que dividen la
	ventana del navegador en columnas usamos el atributo <emphasis>COLS
	</emphasis>de la etiqueta FRAMESET. Este atributo toma como valores
	separados por comas la anchura de los diferentes frames:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAMESET COLS="ancho_col1, ancho_col2, ..."&gt;
	</sgmltag>

      </programlisting>

      <para>
	Existen diversas formas de indicar el ancho de las columnas, una de
	ellas es usando porcentajes. Con este método podríamos dividir la
	ventana en dos columnas usando el siguiente código:
      </para>

      <programlisting>

	<sgmltag>&lt;HTML&gt;</sgmltag>

	<sgmltag>&lt;HEAD&gt;</sgmltag>
	<sgmltag>&lt;/HEAD&gt;</sgmltag>

	<sgmltag>&lt;FRAMESET COLS="20%,80%"&gt;</sgmltag>
	<sgmltag>&lt;/FRAMESET&gt;</sgmltag>

	<sgmltag>&lt;/HTML&gt;</sgmltag>

      </programlisting>

      <para>
	En la que estamos diciendo que la primera columna (la de la
	izquierda) ocupará el 20% del ancho de la ventana del navegador y
	la segunda ocupará el 80% (ver figura 8.9).
      </para>

      <figure label="8.9">

	<title>
	  Como vemos la ventana del navegador ha quedado dividida en dos
	  columnas con un ancho del 20% y del 80% del ancho total de la
	  ventana.
	</title>
	<graphic fileref="img/fig8-09.png"/>

      </figure>

      <warning>
	<para>
	  Si el lector decide probar el ejemplo anterior probablemente no
	  obtenga el resultado deseado en su navegador. Esto es así porque
	  por ahora sólo hemos dicho que queremos que la ventana del
	  navegador se divida en dos columnas pero para indicar que serán
	  frames (lo cual es necesario para obtener el resultado de la figura
	  8.9) será necesario añadir algo más de código que veremos un poco
	  más adelante.
	</para>
      </warning>

      <para>
	Si quisiéramos dividir la ventana en 5 columnas simplemente
	deberíamos especificar cinco valores separados por comas, por
	ejemplo:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAMESET COLS="10%,50%,15%,20%,5%"&gt;
	</sgmltag>

      </programlisting>

      <para>
	Por supuesto la suma de todos los porcentajes debe ser igual a 100.
      </para>

      <note>

	<para>
	  El lenguaje HTML no fija ningún límite en el número de filas,
	  columnas o frames en general en que puede dividirse una página.
	  Sin embargo no es recomendable crear más de 4 frames en total,
	  porque si no el espacio dedicado a cada uno de ellos será muy
	  pequeño. Debemos recordar que muchos de los navegantes verán
	  nuestras páginas con un monitor de 14 pulgadas y una resolución
	  de 640x480.
	</para>

      </note>

      <para>
	El método de especificar los tamaños de las columnas en porcentajes
	es sencillo de entender, sin embargo no siempre es útil. Por ello
	existen otros métodos que es importante comentar.
      </para>

      <para>
	Uno de ellos consiste en especificar el ancho de las columnas en su
	tamaño absoluto en pixeles directamente. Sin embargo existe un
	problema: existen varias resoluciones de pantalla distintas y sería
	muy extraño que todos los navegantes que visiten nuestras páginas
	tengan una misma anchura de pantalla. Por esta razón no podemos
	saber cuál será el ancho total de la ventana del navegador y esto
	sería imprescindible para poder indicar el ancho de las columnas en
	pixeles. Para solucionar este problema <productname>Netscape
	</productname> invento el asterisco (*). Cuando se pone un
	asterisco como ancho de una columna estamos indicando al navegador
	que use todo el espacio sobrante para ese frame. Por ejemplo si
	escribimos:
      </para>

      <programlisting>

	<sgmltag>&lt;FRAMESET COLS="100,300,*"&gt;</sgmltag>

      </programlisting>

      <para>
	se crearán tres columnas, la primera de 100 pixeles, la segunda de
	300 y la tercera ocupará todo el espacio que reste y que dependerá
	del tamaño de la ventana del navegador con el que se visualicen las
	páginas. Si el navegante usa una configuración de 640x480 es de
	esperar que la ventana de su navegador tenga un ancho entre 600 y
	640 pixeles y la tercera columna tendría una anchura de 200-240
	pixeles. Si tiene una configuración de 800x600 el ancho será
	760-800 pixeles y por tanto la tercera columna ocuparía en este
	caso 360-400 pixeles de ancho. Esta es la resolución que hemos
	usado para obtener la figura 8.8.
      </para>

      <figure label="8.10">

	<title>
	  Las dos primeras columnas ocupan un ancho absoluto de 100 y 300
	  pixeles. La tercera al haber usado el asterisco ocupará el resto,
	  que en este ejemplo son 372 pixeles.
	</title>
	<graphic fileref="img/fig8-10.png"/>

      </figure>

      <para>
	Estas son las dos configuraciones más extendidas y las que por
	tanto debemos tener en cuenta. Si queremos cuidar mucho estos
	detalles debemos tener en cuenta las configuraciones de 1024x800 o
	mayores y el hecho de que los usuarios pueden cambiar el tamaño de
	la ventana del navegador a su antojo. Sin embargo, por lo general,
	no es en absoluto necesario llegar hasta esos extremos de cuidado.
      </para>

      <para>
	El siguiente método para especificar el ancho de las columnas
	consiste en combinar todos los anteriores. La mejor forma de
	repasarlos es estudiando unos cuantos ejemplos. Para ellos vamos a
	suponer un navegador de 800 pixeles de ancho:
      </para>

      <orderedlist>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET COLS="*,*"&gt;</sgmltag> : Al
	    usar dos veces el asterisco se divide <emphasis>el resto
	    </emphasis> entre las dos columnas a partes iguales. Es decir,
	    cada una ocupará la mitad de la ventana del navegador (unos 400
	    pixeles cada una). Este ejemplo es idéntico a usar <sgmltag>&lt;
	      FRAMESET COLS="50%,50%"&gt;</sgmltag>.
	  </para>

	</listitem>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET COLS="100,*,*"&gt;</sgmltag> : Divide la
	    ventana en tres columnas. La primera de 100 pixeles y entre las
	    otras dos divide a partes iguales el espacio sobrante (700
	    pixeles) con lo que (suponiendo el ancho total de 800 pixeles)
	    cada una tendrá un ancho de 350 pixeles.
	  </para>

	  <note>

	    <para>
	      Algunos de estos ejemplos requieren algún tiempo de práctica.
	      En caso de tener alguna dificultad entendiendo alguno de
	      ellos es mejor seguir practicando con frames usando los
	      métodos algo menos complejos. Una vez hayamos adquirido más
	      práctica trabajando con frames podremos volver sobre estos
	      ejemplos y para practicar con ellos.
	    </para>

	  </note>

	</listitem>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET COLS="200,2*,*"&gt;</sgmltag>: La primera
	    columna ocupará un tamaño absoluto de 200 pixeles. El resto
	    (600 pixeles) es divido entre las dos siguientes columnas, pero
	    ahora no será a partes iguales, sino que la columna central
	    debe ser el doble que la columna izquierda (este detalle se
	    indica anteponiendo un 2 al asterisco). Por tanto del espacio
	    sobrante, dos tercios (que equivalen a 400 pixeles en este
	    caso), serán para la columna central y un tercio (200 pixeles)
	    será para la tercera.
	  </para>

	  <para>
	    Si hubiésemos querido que una columna fuese el triple de la
	    otra deberíamos haber usado el código COLS="200,3*,
	    *". Y si quisiéramos una relación 4:5 no tendríamos más
	    que escribir COLS="200,4*,5*".
	  </para>

	</listitem>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET COLS="100,50%,*"&gt;</sgmltag>:
	    La primera columna ocupa 100 pixeles, la segunda la mitad del
	    ancho de la pantalla (400 pixeles) y la tercera el resto, que
	    en este caso será 300 pixeles. En la figura 8.11 podemos ver
	    un ejemplo en el que se realiza esta división de columnas en un
	    ordenador con configuración 800x600:
	  </para>

	  <figure label="8.11">

	    <title>
	      En esta ocasión vemos un ejemplo del uso de FRAMESET
	      combinando los distintos modos de indicar el tamaño y el
	      ancho resultante de cada uno para una ventana con 800 pixeles
	      de ancho.
	    </title>
	    <graphic fileref="img/fig8-11.png"/>

	  </figure>

	  <note>

	    <para>
	      Los tamaños absolutos deben usarse sólo en ocasiones que
	      realmente lo requieran y siempre que los utilicemos debe
	      haber al menos una de las otras columnas con un tamaño
	      definido usando un asterisco (*).
	    </para>

	  </note>

	</listitem>

      </orderedlist>

    </sect2>

    <sect2>

      <title>División en filas</title>

      <para>
	Hasta ahora hemos aprendido a dividir la ventana en columnas, pero
	como es de esperar también se puede dividir en filas. En esta
	ocasión será necesario usar el atributo <emphasis>ROWS</emphasis>
	(<emphasis>rows</emphasis> significa precisamente <emphasis>filas
	</emphasis>en inglés). El funcionamiento del atributo ROWS es muy
	similar al de COLS:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAMESET ROWS="alto_fila1, alto_fila2, ..."&gt;
	</sgmltag>

      </programlisting>

      <warning>

	<para>
	  Es importante hacer notar que cuando dividimos la ventana en
	  filas y usamos tamaños relativos, estos se refieren al alto de la
	  ventana, y no tienen en cuenta lo <emphasis>larga</emphasis> que
	  puedan ser las páginas o si hay que usar las barras de
	  desplazamiento o no para moverse por ella.
	</para>

      </warning>

      <para>
	En este caso es más importante, si cabe, tener en cuenta que ni
	podemos saber el alto de la ventana del navegador de aquellos que
	visiten nuestras páginas. Por esta razón el uso de tamaños
	relativos o del asterisco (*) es aún más usual. Veamos algunos
	ejemplos:
      </para>

      <orderedlist>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET ROWS="200,*,100"&gt;</sgmltag>:
	    Divide la ventana en tres filas: una superior de 200 pixeles en
	    la que se podría poner un encabezado; una inferior de 100
	    pixeles en la que puede ponerse una coletilla o una barra de
	    navegación; y una central que ocupa el espacio sobrante y donde
	    se podría situar el contenido principal.
	  </para>

	</listitem>

	<listitem>

	  <para>
	    <sgmltag>&lt;FRAMESET ROWS="15%,*,3*"&gt;</sgmltag>:
	    Se divide la ventana en una primera fila que ocupa el 15% de la
	    altura; la siguiente fila ocupará un cuarto del resto y la
	    tercera tres cuartos.
	  </para>

	</listitem>

      </orderedlist>

      <para>
	No son necesarios más ejemplos para comprender el funcionamiento,
	así que vamos ya a definir el contenido de cada una de las
	divisiones que hemos aprendido a crear. Cuando insertemos este
	contenido, la división se convertirá en lo que realmente se llama
	frame. Mientras tanto ésta estará sujeta a más divisiones como
	veremos un poco más adelante.
      </para>

    </sect2>

    <sect2>

      <title>Combinando COLS y ROWS</title>

      <para>
	Los atributos COLS y ROWS pueden ser usados simultáneamente en una
	misma etiqueta FRAMESET. Si hacemos esto dividiremos la ventana del
	navegador en cuadrículas de diferentes tamaños. Podríamos
	interpretar el resultado de usar ambos atributos simultáneamente
	como que cada una de las filas definidas por ROWS es subdividida en
	el número de columnas que indica COLS. La página que mostramos en
	la figura 8.1 es una muestra del uso de COLS y ROWS
	simultáneamente. En ese caso el código usado fue:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAMESET COLS="*,*" ROWS="*,*"&gt;
	</sgmltag>
	...
	<sgmltag>&lt;/FRAMESET&gt;</sgmltag>

      </programlisting>

      <para>
	También se pueden crear combinaciones más complejas como por
	ejemplo:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAMESET COLS="100,50,*,*"
	  ROWS="15%,*,3*"&gt;
	</sgmltag>
	... Más código ...
	<sgmltag>&lt;/FRAMESET&gt;</sgmltag>

      </programlisting>

      <para>
	Que da lugar a la página de la figura 8.12:
      </para>

      <figure label="8.12">

	<title>
	  Es posible usar los atributos COLS y ROWS simultáneamente para
	  dividir la ventana en varias filas y columnas.
	</title>
	<graphic fileref="img/fig8-12.png"/>

      </figure>

    </sect2>

    <sect2>

      <title>La etiqueta FRAME</title>

      <para>
	La etiqueta usada para insertar el contenido en las filas, columnas
	o divisiones en general, hechas con la etiqueta FRAMESET, es
	<emphasis>FRAME</emphasis>. Esta etiqueta debe ser usada una vez
	por cada una de las divisiones que hallamos creado. Cada una de
	estas veces debemos usar un atributo, <emphasis>SRC</emphasis>, que
	servirá para indicar el documento HTML donde se encuentre el
	contenido que queremos insertar en esa división:
      </para>

      <programlisting>

	<sgmltag>&lt;FRAME SRC="docum.htm"&gt;</sgmltag>

      </programlisting>

      <para>
	Esta etiqueta consta de una única instrucción, es decir la
	instrucción de fin <sgmltag>&lt;/FRAME&gt;</sgmltag> no existe, y
	debe ser insertada entre la instrucción de inicio y la instrucción
	de fin de la etiqueta FRAMESET.
      </para>

      <para>
	Otro de los atributos más importantes de la etiqueta FRAME es
	<emphasis>NAME</emphasis>. Con este atributo le daremos un nombre
	al frame para poder referirnos posteriormente a él con el atributo
	TARGET o con los lenguajes de script. La forma de usar este
	atributo es:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;FRAME SRC="docum.htm"
	  NAME="primer_frame"&gt;
	</sgmltag>

      </programlisting>

      <para>
	Si posteriormente queremos crear un enlace a este frame desde otro
	de la misma página no tendremos más que escribir:
      </para>

      <programlisting>

	<sgmltag>
	  &lt;A HREF="otro.htm"
	  TARGET="primer_frame"&gt;
	</sgmltag>

      </programlisting>

      <para>
	Con los conocimientos adquiridos hasta ahora en este capítulo,
	junto con lo que ya sabemos, estamos en disposición de crear
	nuestra primera página completa con frames.
      </para>

    </sect2>

  </sect1>

  <sect1>

    <title>Página completa con frames</title>

    <para>
      El primer paso será crear el documento de definición de frames.
      Abrimos nuestro editor y creamos un nuevo archivo. En este caso lo
      llamaremos <filename>index.htm</filename> y
      escribimos en él el siguiente código:
    </para>

    <programlisting>

      <sgmltag>&lt;HTML&gt;</sgmltag>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      <sgmltag>&lt;TITLE&gt;</sgmltag>
      Primera página con frames
      <sgmltag>&lt;/TITLE&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

      <sgmltag>&lt;FRAMESET COLS="20%,80%"&gt;</sgmltag>
      <sgmltag>&lt;FRAME SRC="izquierd.htm"&gt;</sgmltag>
      <sgmltag>&lt;FRAME SRC="derecha.htm"&gt;</sgmltag>
      <sgmltag>&lt;/FRAMESET&gt;</sgmltag>

      <sgmltag>&lt;/HTML&gt;</sgmltag>

    </programlisting>

    <para>
      Con esto tendremos una página que dividirá la ventana del navegador
      en una columna izquierda y otra derecha que ocupan el 20% y el 80%
      del ancho respectivamente. El contenido de cada una de estas columnas
      estará en los archivos <filename>izquierda.htm</filename> y
      <filename>derecha.htm</filename>. 
      El siguiente paso será la creación de estos archivos. Volvemos a
      abrir un archivo nuevo en nuestro editor y le llamamos
      <filename>izquierd.htm</filename>. En este archivo podemos 
      escribir todo el código HTML que queramos siempre teniendo en cuenta
      sus limitadas dimensiones. Para empezar podemos escribir:
    </para>

    <programlisting>

      <sgmltag>&lt;HTML&gt;</sgmltag>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

      <sgmltag>&lt;BODY BGCOLOR="#99FF99"&gt;</sgmltag>
      <sgmltag>&lt;H1&gt;</sgmltag>
      Frame izquierdo
      <sgmltag>&lt;/H1&gt;</sgmltag>
      Este frame podría servir de índice:
      <sgmltag>&lt;UL&gt;</sgmltag>
      <sgmltag>&lt;LI&gt;</sgmltag>Tema 1
      <sgmltag>&lt;LI&gt;</sgmltag>Tema 2
      <sgmltag>&lt;LI&gt;</sgmltag>Tema 3
      <sgmltag>&lt;/UL&gt;</sgmltag>
      <sgmltag>&lt;/BODY&gt;</sgmltag>

      <sgmltag>&lt;/HTML&gt;</sgmltag>

    </programlisting>

    <para>
      A continuación abrimos otro archivo nuevo y le llamamos
      <filename>derecha.htm</filename>. El código a incluir en 
      este tercer documento HTML será:
    </para>

    <programlisting>

      <sgmltag>&lt;HTML&gt;</sgmltag>

      <sgmltag>&lt;HEAD&gt;</sgmltag>
      <sgmltag>&lt;/HEAD&gt;</sgmltag>

      <sgmltag>&lt;BODY&gt;</sgmltag>
      <sgmltag>&lt;H1&gt;</sgmltag>
      Este es el frame Derecho
      <sgmltag>&lt;/H1&gt;</sgmltag>
      <sgmltag>&lt;P&gt;</sgmltag>
      En este frame podría ir el contenido principal del Web.
      <sgmltag>&lt;/P&gt;</sgmltag>
      <sgmltag>&lt;/BODY&gt;</sgmltag>

      <sgmltag>&lt;/HTML&gt;</sgmltag>

    </programlisting>

    <para>
      El último paso será visualizar el documento de definición de frames,
      <filename>index.htm</filename>, con el navegador
      que prefiramos. Por supuesto se podría ver cada uno de los documentos
      HTML por separado, y de hecho suele ser muy útil hacerlo mientras
      escribamos el código para ir comprobando que lo estamos haciendo bien,
      sin embargo una vez hemos terminado los documentos
      <filename>derecha.htm</filename> y
      <filename>izquierd.htm</filename> sólo tienen significado como 
      contenido de frames, y no como páginas Web individuales. Por esta
      razón el único archivo que debe ser visto <emphasis>desde
	fuera</emphasis> debe ser el documento de definición de frames
      (en este caso llamado <filename>index.htm</filename>).
      Una vez abierto <filename>index.htm</filename>
      con nuestro navegador deberíamos ver una página similar a la que
      observamos en la figura 8.13.
    </para>

    <figure label="8.13">

      <title>
	Ejemplo completo de una página con frames formado por un frame
	izquierdo pequeño que puede servir de índice y otro derecho con el
	contenido principal.
      </title>
      <graphic fileref="img/fig8-13.png"/>

    </figure>

    <para>
      La creación de páginas con frames es algo más compleja que la
      creación de las páginas que hemos hecho hasta ahora, y por ello es
      común cometer errores, sobre todo al principio. Si al intentar ver
      una página con frames no observamos el resultado obtenido es
      recomendable comprobar los siguientes detalles:
    </para>

    <orderedlist>

      <listitem>

	<para>
	  Al igual que con el resto de páginas debemos asegurarnos de haber
	  encerrado todo el código entre las instrucciones <sgmltag>&lt;
	    HTML&gt;</sgmltag> y <sgmltag>&lt;/HTML&gt;</sgmltag> y de que
	  hemos guardado todos los archivos con terminación <emphasis>.htm
	  </emphasis>o <emphasis>.html</emphasis>.
	</para>

      </listitem>

      <listitem>

	<para>
	  Comprobar que los nombres de los archivos coinciden con los
	  nombres indicados como valor del atributo SRC. En algunos casos
	  es necesario que los nombres coincidan en mayúsculas y
	  minúsculas. Este será el caso si vamos a publicar nuestras
	  páginas en un servidor UNIX (la mayoría lo son).
	</para>

      </listitem>

      <listitem>

	<para>
	  Es importante que nos aseguremos de no haber usado la etiqueta
	  BODY en el archivo <filename>index.htm</filename>. Si lo hemos hecho debemos quitarla para que funcione
	  nuestro ejemplo.
	</para>

      </listitem>

      <listitem>

	<para>
	  Si hemos solucionado todos los problemas anteriores y la página
	  sigue sin visualizarse correctamente probablemente estaremos
	  usando un navegador que no soporte frames.
	</para>

      </listitem>

    </orderedlist>

  </sect1>

  <sect1>

    <title>Atributos de la etiqueta FRAME</title>

    <para>
      Además del atributo SRC y NAME, la etiqueta FRAME tiene otros que nos
      permitirán modificar algunas características de los frames. Todos los
      atributos  que veremos a continuación han sido aceptados en el
      estándar HTML 4.0.
    </para>

    <itemizedlist>

      <listitem>

	<para>
	  <emphasis>SCROLLING</emphasis>: Usando este atributo podemos
	  controlar la aparición o no de barras de desplazamiento. Los
	  valores que puede tomar este atributo son:
	</para>

	<itemizedlist>

	  <listitem>

	    <para>
	      SCROLLING="auto": Este es el valor por defecto y
	      provoca que el navegador sólo inserte barras de
	      desplazamiento si el contenido del frame ocupa más del
	      espacio dedicado para él.
	    </para>

	  </listitem>

	  <listitem>

	    <para>
	      SCROLLING="yes": Las barras de desplazamiento
	      aparecerán siempre.
	    </para>

	  </listitem>

	  <listitem>

	    <para>
	      SCROLLING="no": Las barras de desplazamiento no
	      aparecerán nunca. Hay que tener mucho cuidado si usamos esta
	      opción porque si la página ocupa más que el espacio de la
	      ventana dedicado al frame la información que no quepa en un
	      principio no podrá ser vista.
	    </para>

	  </listitem>

	</itemizedlist>

      </listitem>

      <listitem>

	<para>
	  <emphasis>NORESIZE</emphasis>: Por defecto los usuarios pueden
	  mover los bordes de los frames sin más que situar el ratón sobre
	  ellos y arrastrarlos. Si incluimos este atributo en la etiqueta
	  FRAME evitaremos que el borde de ese frame pueda ser movido. Es
	  necesario notar que este atributo no toma ningún valor.
	</para>

      </listitem>

      <listitem>

	<para>
	  <emphasis>MARGINHEIGHT</emphasis>: Ajusta el margen superior que
	  debe respetar el contenido de ese frame. El tamaño debe ser dado
	  en pixeles.
	</para>

      </listitem>

      <listitem>

	<para>
	  <emphasis>MARGINWIDTH</emphasis>: Ajusta el margen lateral que
	  debe respetar el contenido del frame. Al igual que antes, el
	  margen debe ser dado en pixeles.
	</para>

      </listitem>

    </itemizedlist>

    <para>
      Para terminar vamos a ver un ejemplo del uso de todos ellos:
    </para>

    <programlisting>

      <sgmltag>
	&lt;FRAMESET COLS="110,*" ROWS="110,*"&gt;
      </sgmltag>
      <sgmltag>
	&lt;FRAME NAME="Imagen"
	SRC="cuadrado.gif"
	MARGINWIDTH="0"
	MARGINHEIGHT="0"
	SCROLLING="No"
	FRAMEBORDER="No"
      </sgmltag>
      <sgmltag>&lt;FRAME SRC="f2.htm"</sgmltag>
      <sgmltag>
	&lt;FRAME SRC="rect.gif" SCROLLING="no"&gt;
      </sgmltag>
      <sgmltag>
	&lt;FRAME SRC="f4.htm"
	MARGINWIDTH="100"
	MARGINHEIGHT="100"
      </sgmltag>
      <sgmltag>&lt;/FRAMESET&gt;</sgmltag>

    </programlisting>

    <para>
      que puede ser visto en la figura 8.14:
    </para>

    <figure label="8.14">

      <title>
	Usando los diferentes atributos de la etiqueta FRAME podemos tener
	un mayor control sobre las diferentes características de los frames.
      </title>
      <graphic fileref="img/fig8-14.png"/>

    </figure>

    <para>
      En esta figura observamos, entre otras cosas, que aunque la figura
      <filename>rect.gif</filename> no cabe en el
      tercer frame (abajo izquierda) no aparecen las barras de
      desplazamiento. Otros aspectos interesantes están comentados en rojo
      sobre la figura.
    </para>

    <note>

      <para>
	En general es muy útil establecer unos márgenes iguales a cero en
	los frames cuya función sea la de barra de navegación. En los
	frames con contenido, especialmente si este es texto, se suele
	poner un margen de 10 pixeles.
      </para>

    </note>

  </sect1>

  <sect1>

    <title>Compatibilidad: NOFRAME</title>

    <para>
      Hasta ahora hemos supuesto que los navegantes que vieran nuestras
      páginas con navegadores con soporte para frames, sin embargo ¿qué
      ocurre si las páginas son visualizadas con un navegador que no los
      soporte?. Este es un aspecto clave a considerar porque hoy por hoy
      los frames no forman parte del estándar y aunque <productname>
	Explorer</productname>, <productname>Netscape</productname> y
      <productname>Opera</productname> los entienden hay muchos otros que
      no lo hacen.
    </para>

    <para>
      Afortunadamente los inventores de los frames tuvieron este aspecto en
      cuenta y crearon una nueva etiqueta, <emphasis>NOFRAME</emphasis>,
      pensada para este tipo de navegadores. Esta etiqueta debe ser usada
      entre <sgmltag>&lt;FRAMESET&gt;</sgmltag> y <sgmltag>&lt;/FRAMESET&gt;
      </sgmltag> de la siguiente manera:
    </para>

    <programlisting>

      <sgmltag>&lt;FRAMESET ...&gt;</sgmltag>
      ...Código de definición de frames...
      <sgmltag>&lt;NOFRAME&gt;</sgmltag>
      ...Código alternativo...
      <sgmltag>&lt;/NOFRAME&gt;</sgmltag>
      <sgmltag>&lt;/FRAMESET&gt;</sgmltag>

    </programlisting>

    <para>
      Los navegadores que tengan soporte para frames simplemente ignorarán
      todo lo que haya entre la instrucción de inicio, <sgmltag>&lt;NOFRAME
	&gt;</sgmltag>, y la instrucción de fin, <sgmltag>&lt;/NOFRAME&gt;
      </sgmltag>. Por su parte los navegadores que no entiendan frames,
      directamente ignorarán todas estas etiquetas (no las entienden) y
      pasarán a interpretar las etiquetas que siguen a <sgmltag>&lt;NOFRAME
	&gt;</sgmltag>. Entre ellas debe estar la etiqueta BODY, ya que esta
      etiqueta era imprescindible para las páginas sin frames, y para los
      navegadores que no las soporten todas las páginas son sin frames. En
      esta ocasión BODY no provocará problemas con FRAMESET porque, como
      hemos dicho, los navegadores que entiendan esta última ignorarán todo
      el código entre <sgmltag>&lt;NOFRAME&gt;</sgmltag> y <sgmltag>&lt;
	/NOFRAME&gt;</sgmltag>.
    </para>

    <para>
      Un uso habitual de NOFRAME es:
    </para>

    <programlisting>

      <sgmltag>&lt;FRAMESET COLS="200,*"&gt;</sgmltag>

      <sgmltag>&lt;FRAME SRC="f1.htm"&gt;</sgmltag>
      <sgmltag>&lt;FRAME SRC="f2.htm"&gt;</sgmltag>

      <sgmltag>&lt;NOFRAME&gt;</sgmltag>

      <sgmltag>&lt;BODY&gt;</sgmltag>
      <sgmltag>&lt;P&gt;</sgmltag>Esta página usa frames y su
      navegador no los soporta, por favor vaya a
      <sgmltag>&lt;A HREF="sinframe.htm"&gt;</sgmltag>
      nuestras páginas sin frames
      <sgmltag>&lt;/A&gt;</sgmltag>
      <sgmltag>&lt;/BODY&gt;</sgmltag>

      <sgmltag>&lt;/NOFRAME&gt;</sgmltag>

      <sgmltag>&lt;/FRAMESET&lt;</sgmltag>

    </programlisting>

    <para>
      Si no hubiéramos usado la etiqueta NOFRAME el resultado de ver la
      página con este código con un navegador sin soporte para frames sería
      simplemente una pantalla en blanco como la que vemos en la figura
      8.15:
    </para>

    <figure label="8.15">

      <title>
	El navegador <productname>NCSA Mosaic</productname> no soporta
	frames, con lo que los usuarios de dicho navegador no verán nada si
	no usamos la etiqueta NOFRAME.
      </title>
      <graphic fileref="img/fig8-15.png"/>

    </figure>

    <para>
      usándola se obtendrá el resultado mucho más explicativo de la figura
      8.16.
    </para>

    <figure label="8.16">

      <title>
	En este caso sí hemos usado NOFRAMES ofreciendo al navegante un
	enlace para ver una versión sin frames de las páginas.
      </title>
      <graphic fileref="img/fig8-16.png"/>

    </figure>

  </sect1>
  <sect1>
    <title>Anidamiento de framesets</title>
    <para>
      Hasta ahora hemos aprendido a dividir la ventana del navegador en dos o 
      más filas con el atributo <sgmltag
					 class="attribute">ROW</sgmltag>, sin embargo las posibilidades
      del lenguaje HTML no acaban ahí.
    </para>
    <para>
      Tanto a lo largo de este capítulo, como en el capítulo anterior hemos 
      ido viendo algunos ejemplos de frames (figuras 7.1, 7.6 y 8.2) que 
      presentaban páginas Web que dividían la pantalla del navegador de una 
      forma más compleja que simplemente dividir en filas o columnas. 
      ¿Cómo lo hacen? La respuesta es simple, anidando
      <sgmltag>FRAMESET</sgmltag>s.  
      En la figura 8.17 podemos ver una
      explicación gráfica de lo que esto significa. Vamos a explicarla
      paso a paso.  
    </para>

    <figure id="fig8-17" label="8.17" xreflabel="figura 8.17">
      <title>Para conseguir estructuras de frames más complejas puede
	anidarse la etiqueta FRAMESET. En esta figura se indica cómo hacerlo.</title>
      <graphic fileref="img/fig8-17.png"/>
    </figure>

    <para>
      Empezamos con el siguiente código:      
    </para>

    <programlisting format="linespecific">

      <sgmltag>&lt;frameset cols=&quot;150,*&quot;&gt;</sgmltag>
      <sgmltag>... más código ...</sgmltag>
      <sgmltag>&lt;frameset&gt;</sgmltag>

    </programlisting>

    <para>
      Con él estamos dividiendo la ventana del navegador en dos columnas. 
      Como hemos visto un poco antes en este capítulo podemos insertar 
      contenido en cada una de las divisiones creadas usando la etiqueta 
      FRAME, por ejemplo:
    </para>

    <programlisting format="linespecific">
      
      <sgmltag>&lt;frameset cols=&quot;150,*&quot;&gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame1.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame2.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;frameset&gt;</sgmltag>

    </programlisting>

    <para>
      Pero existe otra posibilidad. En vez de usar la etiqueta FRAME 
      para insertar contenido en la segunda de las divisiones podemos 
      volver a usar la etiqueta <sgmltag>FRAMESET</sgmltag>
      (anidándola dentro de la anterior  
      aparición de esta misma etiqueta) para subdividir la parte derecha 
      en dos, es decir:
    </para>

    <programlisting format="linespecific">

      <sgmltag>&lt;frameset cols=&quot;150,*&quot;&gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame1.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;frameset rows=&quot;100,*&quot;&gt;</sgmltag>
      ... Más código ...
      <sgmltag>  &lt;/frameset&gt;</sgmltag>
      <sgmltag>&lt;/frameset&gt;</sgmltag>

    </programlisting>

    <para>
      Ahora tendremos una página Web formada por tres frames. El primero 
      esta situado a la izquierda de la pantalla y tendrá 200 pixeles de 
      ancho. Los otros dos ocupan el espacio sobrante. Uno ocupa los 100 
      pixeles superiores y el otro el resto. Por supuesto, al haber dos 
      frames más, debemos volver a usar la etiqueta
      <sgmltag>FRAME</sgmltag> para insertar  
      contenido en ellos. El código total quedaría por tanto.
    </para>

    <programlisting format="linespecific">

      <sgmltag>&lt;frameset cols=&quot;150,*&quot;&gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame1.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;frameset rows=&quot;100,*&quot; &gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame21.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;frame src=&quot;frame22.htm&quot;/&gt;</sgmltag>
      <sgmltag>&lt;/frameset&gt;</sgmltag>
      <sgmltag>&lt;/frameset&gt;</sgmltag>

    </programlisting>

    <tip>
      <para>
	No existe ningún límite en el número de niveles de anidamiento 
	de la etiqueta <sgmltag>FRAMESET</sgmltag> que pueden
	usarse. La única limitación que  
	debemos tener en cuenta es el tamaño y resolución de pantalla que 
	usarán los navegantes para ver nuestras páginas. Si usamos muchos 
	anidamientos los frames serán muy pequeños y se podrá ver poca 
	información en cada uno de ellos, con lo que se hará necesario usar 
	las molestas barras de desplazamiento. 
      </para>
    </tip>

    <para>
      El hecho de haber elegido esta estructura de tres frames para el 
      ejemplo no es casualidad. Como hemos visto en las capturas de páginas 
      reales mostradas hasta ahora, es una de las más usadas. El frame 
      izquierdo puede usarse como índice y el superior como un espacio para 
      incluir un banner o encabezado gráfico. Estos dos frames pueden mantener
      su contenido constante mientras en el tercero y más grande de los frames
      se presenta el contenido principal de la página y es sobre el que se va 
      navegando.
    </para>

    <para>
      Para tener la página Web completa sólo nos quedaría crear los 
      archivos <ulink
		      url="extra/ejem18/frame1.htm"><filename>extra/ejem18/frame1.htm</filename></ulink>,  
      <ulink url="extra/ejem18/frame21.htm"><filename>extra/ejem18/frame21.htm</filename></ulink> y 
      <ulink
	     url="extra/ejem18/frame22.htm"><filename>extra/ejem18/frame22.htm</filename></ulink>.
      Nosotros hemos creado  
      unos de ejemplo, incluyendo un encabezado gráfico para el frame superior
      y hemos obtenido el resultado que vemos en la fig8-18. 
    </para>

    <figure id="fig8-18" label="8.18" xreflabel="figura 8.18">
      <title>Nuestra primera página con frames. La ventana está dividida
	en 3 apartados (los frames) el izquierdo es el índice, el superior el
	título y el central el que tiene el contenido.</title>
      <graphic fileref="img/fig8-18.png"/>
    </figure>

    <para>
      Todos los aspectos relacionados con los frames que hemos visto hasta 
      ahora son iguales para todos los navegadores y han sido adoptados en 
      el estándar HTML 4.0. Esto quiere decir que son entendidos por los 
      navegadores Microsoft Internet Explorer 3.0 o posterior, Netscape 
      Navigator 2.0 o posterior, Opera 3.0 o posterior y los navegadores 
      gráficos que aparezcan de ahora en adelante con soporte completo de 
      HTML 4.0.
    </para>
    <para>
      Debemos recordar que los frames fueron una invención de Netscape y 
      posteriormente Explorer y otros lo adoptaron. Sin embargo también 
      cambiaron algunas características. Por esta razón en los atributos y 
      etiquetas que veamos a partir de ahora haremos mención explícita de que 
      diferencias existen entre la interpretación del código de los frames 
      por parte de Netscape y Explorer, y lo indicaremos cuando alguna 
      etiqueta, atributo o valor determinado haya sido adoptado por el 
      estándar HTML 4.0.
    </para>

  </sect1>

  <sect1>
    <title>Bordes de los frames</title>

    <para>
      Cuando creamos páginas con frames estos aparecen divididos por unas 
      líneas horizontales y verticales (ver 8-19) que 
      además el usuario que vea nuestra página podrá mover a su antojo 
      (a no ser que usemos el atributo <sgmltag
						class="attribute">NORESIZE</sgmltag>).  
    </para>

    <figure id="fig8-19" label="8.19" xreflabel="figura 8.19">
      <title>En general los navegadores dibujan líneas con aspecto 3-D
	para separar los frames.</title>
      <graphic fileref="img/fig8-19.png"/>
    </figure>

    <para>
      Desde la aparición de los frames los programadores de páginas 
      Web han estado reclamando un mayor control sobre la disposición 
      sobre estos bordes y con la aparición de Netscape 3.0 e Internet 
      Explorer 3.0 se introdujeron nuevos métodos para definir sus 
      características. 
    </para>
    <para>
      Estos métodos no son más que la inclusión de tres nuevos atributos 
      para las etiquetas <sgmltag>FRAME</sgmltag> y
      <sgmltag>FRAMESET</sgmltag>. Estos son <sgmltag
						      class="attribute">BORDERCOLOR</sgmltag> y   
      <sgmltag class="attribute">FRAMEBORDER</sgmltag> para
      <sgmltag>FRAME</sgmltag> y <sgmltag
					  class="attribute">BORDER</sgmltag> para
      <sgmltag>FRAMESET</sgmltag>.  
      Aprendamos a usarlos uno por uno.
    </para>

    <sect2>
      <title>Cambiando el color del borde</title>
      <para>
	El primero de los atributos, <sgmltag
					      class="attribute">BORDERCOLOR</sgmltag>, nos permitirá cambiar
	el color del borde. El color puede indicarse tanto con el
	número hexadecimal RGB (por ejemplo #99CC99) o bien usando uno
	de los nombres predeterminados que vimos en el capítulo 5. Por
	ejemplo para poner el borde de color rojo podríamos usar:
	<programlisting format="linespecific">
	  &lt;FRAME BORDERCOLOR="red"&gt;
	</programlisting>
	O bien:
	<programlisting format="linespecific">
	  &lt;FRAME BORDERCOLOR="#FF0000"&gt;
	</programlisting>
      </para>
      <para>
	Dado que los bordes son compartidos por varios frames, en el
	caso de que cada uno de los bordes indique un color diferente
	el resultado es impredecible y depende del navegador. Por esta
	razón no podemos fiarnos de que si se da esta situación lo que
	nosotros con nuestro navegador será lo mismo que vean los
	navegantes y por tanto debemos evitarla en lo posible.
      </para>
      <para>
	Si en cada uno de los frames de las páginas creadas en la
	sección anterior usamos este atributo para indicar bordes
	rojos obtendremos el resultado de la figura 8.20. El código
	usado es:
      </para>
      <programlisting format="linespecific">
	&lt;FRAMESET COLS="150,*">
	&lt;FRAME SRC="frame1.htm" BORDERCOLOR="#FF0000">
	&lt;FRAMESET ROWS="100,*">
	&lt;FRAME SRC="frame21.htm" BORDERCOLOR="#FF0000">
	&lt;FRAME SRC="frame22.htm" BORDERCOLOR="#FF0000">
	&lt;/FRAMESET>
	&lt;/FRAMESET>
      </programlisting>
      <figure float="0">
	<title>Con el atributo <sgmltag
					class="attribute">BORDERCOLOR</sgmltag> podemos elegir el
	  color que queramos para los bordes de los frames.</title>
	<graphic fileref="img/fig8-20.png"/>
      </figure>
      <para>
	Hay que admitir que el cambio en la apariencia de la página no
	es muy importante, sin embargo puede ser muy útil en ciertos
	Webs en los que los colores están muy cuidados.
      </para>
      <para>
	El navegador Netscape permite también usar el atributo
	<sgmltag class="attribute">BORDERCOLOR</sgmltag> en
	la etiqueta <sgmltag>FRAMESET</sgmltag>, en este caso el valor
	que demos a este atributo afectará a todos los frames dentro
	de ese frameset. De esta manera podríamos habernos evitado
	escribir 
	<sgmltag class="attribute">BORDERCOLOR="#FF0000"</sgmltag>
	tres veces en el ejemplo 
	anterior. Sin embargo debemos tener en cuenta que esta
	posibilidad sólo la ofrece Netscape Navigator y no Explorer ni
	otro navegador. Además tampoco ha sido aceptada por el
	estándar HTML 4.0.
      </para>
    </sect2>
    <sect2>
      <title>Como quitar el borde</title>
      <para>
	Muchos diseñadores de Webs que habían sido detractores de los
	frames cambiaron su opinión sobre ellos una vez se ofreció la
	posibilidad de quitar los bordes de estos. De esta forma se
	puede crear la ilusión de que existe un sólo frame (o una
	página sin frames), mientras aprovechamos las posibilidades
	para mantener siempre visibles ciertas partes de la página.
      </para>
      <para>
	La etiqueta <sgmltag>FRAME</sgmltag> tiene un atributo que
	puede ser usado para quitar los bordes de los frames: 
	<sgmltag class="attribute">FRAMEBORDER</sgmltag>. Este
	atributo puede tomar dos valores: "1" y "0" que significan
	mostrar los bordes y ocultarlos respectivamente. El valor por
	defecto es mostrar los bordes (<sgmltag
						class="attribute">FRAMEBORDER="1"</sgmltag>) y si queremos que
	estos desaparezcan debemos escribir explícitamente:
	<programlisting format="linespecific">
	  &lt;FRAME FRAMEBORDER="0">
	</programlisting>
      </para>
      <para>
	Al igual que antes se pueden producir conflictos si un borde
	es compartido por dos frames uno de los cuales indica
	<sgmltag class="attribute">FRAMEBORDER="0"</sgmltag> y el otro
	<sgmltag class="attribute">FRAMEBORDER="1"</sgmltag>. La
	decisión que tome el navegador en esta situación es
	impredecible y por tanto debe ser evitada en la medida de lo
	posible. 
      </para>
      <para>
 	Aunque no ha sido aceptado por el estándar HTML 4.0, es
	importante hacer notar que tanto Netscape como Explorer
	admiten el uso del atributo <sgmltag
					     class="attribute">FRAMEBORDER</sgmltag> en la etiqueta 
	<sgmltag>FRAMESET</sgmltag>. De esta forma podemos conseguir
	quitar todos los bordes de una vez poniendo <sgmltag
							     class="attribute">FRAMEBORDER="0"</sgmltag> en la primera
	etiqueta <sgmltag>FRAMESET</sgmltag> en lugar de repetirla
	para cada uno de las apariciones de <sgmltag>FRAME</sgmltag>. 
      </para>
      
      <caution>
	<para>
	  El atributo <sgmltag class="attribute">FRAMEBORDER</sgmltag>
	  surgió como extensión al estándar de HTML. Inicialmente los
	  valores válidos eran <sgmltag class="attvalue">yes</sgmltag>
	  y <sgmltag class="attvalue">no</sgmltag>. Sin embargo HTML
	  4.0 introdujo el soporte de frames en el estándar y los
	  valores aceptados de <sgmltag
					class="attribute">FRAMEBORDER</sgmltag> son <sgmltag
											     class="attvalue">1</sgmltag> 
	  y <sgmltag class="attvalue">0</sgmltag>. La conclusión es
	  que aunque en las páginas existentes podamos encontrarnos
	  cualquier combinación de los valores anteriores, en las
	  páginas nuevas deberíamos usar sólo los valores numéricos
	  estándar. 
	</para>
      </caution>
      <figure float="0">
	<title>Usando <sgmltag
			       class="attribute">FRAMEBORDER="0"</sgmltag> ocultamos los
	  bordes de los frames con lo que se consigue una gran mejora en
	  la apariencia.</title>
	<graphic fileref="img/fig8-21.png"/>
      </figure>
      <para>
	En la mayoría de las ocasiones quitando el borde de los frames
	se consigue una apariencia muy mejorada de nuestras
	páginas. Para conseguir este efecto en las páginas creadas en
	el ejemplo anterior sólo tenemos que cambiar ligeramente el
	código para incluir el atributo <sgmltag
						 class="attribute">FRAMEBORDER="0"</sgmltag>: 
	<programlisting format="linespecific">
	  &lt;FRAMESET COLS="150,*"&gt;
	  &lt;FRAME SRC="frame1.htm" FRAMEBORDER="0"&gt;
	  &lt;FRAMESET ROWS="100,*"&gt;
	  &lt;FRAME SRC="frame21.htm" FRAMEBORDER="0"&gt;
	  &lt;FRAME SRC="frame22.htm" FRAMEBORDER="0"&gt;
	  &lt;/FRAMESET&gt;
	  &lt;/FRAMESET&gt;
	</programlisting>
	Con este código obtendríamos el resultado de la figura 8.21 en
	la que no se distinguen los bordes de los frames y por lo
	tanto no se pueden redimensionar. Sin embargo si hacemos la
	ventana del navegador más pequeña aparecerán en cada uno de
	los frames unas barras de desplazamiento que delatarán su
	presencia, como vemos en la figura 8.22.
      </para>
      <figure id="fig8-22" label="8.22" xreflabel="figura 8.22">
	<title>Aún usando el atributo
	  <sgmltag class="attribute">FRAMEBORDER</sgmltag> si hacemos
	  la ventana del navegador pequeña aparecerán barras de
	  desplazamiento en cada uno de los frames.</title>
	<graphic fileref="img/fig8-22.png"/>
      </figure>
    </sect2>
    <sect2>
      <title>Grosor del borde</title>
      <para>
	Una de las últimas extensiones de Netscape Navigator
	relacionadas con los frames es el atributo BORDER. Con él
	podemos indicar el grosor del borde que separa los frames
	asignándole un valor en pixeles. La forma de usar este
	atributo es:
	<programlisting format="linespecific">
	  &lt;FRAMEBORDER BORDER="TamañoBordeEnPixeles"&gt;
	  ...
	  &lt;/FRAMEBORDER&gt;
	</programlisting>
      </para>
      <para>
	Si existen varias etiquetas <sgmltag>FRAMEBORDER</sgmltag> en
	una misma página (es decir si las hemos anidado) podemos usar
	el atributo <sgmltag class="attribute">BORDER</sgmltag> en
	cualquiera de ellas, pero es conveniente usarla una 
	única vez. Vamos a usarla con nuestro ejemplo para crear un
	borde grueso entre las dos divisiones del segundo de los
	<sgmltag>FRAMESETS</sgmltag>. Para ello usamos el código:
	<programlisting format="linespecific">
	  &lt;FRAMESET COLS="150,*"&gt;
	  &lt;FRAME SRC="frame1.htm"&gt;
	  &lt;FRAMESET ROWS="100,*" BORDER="20"&gt;
	  &lt;FRAME SRC="frame21.htm"&gt;
	  &lt;FRAME SRC="frame22.htm"&gt;
	  &lt;/FRAMESET&gt;
	  &lt;/FRAMESET&gt;
	</programlisting>
	Con lo que obtenemos el resultado de la figura 8.23.
      </para>
      <figure id="fig8-23" label="8.23" xreflabel="figura 8.23">
	<title>
	  Con el atributo <sgmltag class="attribute">BORDER</sgmltag>
	  podemos cambiar el grosor de los bordes. En este caso el
	  borde horizontal tiene un grosor de 20 pixeles.
	</title> 
	<graphic fileref="img/fig8-23.png"/>
      </figure>
    </sect2>
  </sect1>
  <sect1>
    <title>Valores especiales del atributo
      <sgmltag class="attribute">TARGET</sgmltag>
    </title>
    <para>
      Ya conocemos todo lo que necesitamos sobre los frames, pero
      antes de empezar la siguiente práctica es importante conocer una
      serie de valores de <sgmltag class="attribute">TARGET</sgmltag>
      con significado especial. 
    </para>
    <para>
      Como hemos visto, el atributo
      <sgmltag class="attribute">TARGET</sgmltag> nos permite mostrar
      el resultado de un enlace en el frame que queramos indicando el
      nombre de dicho frame. Existen, además, ciertos valores que
      puede tomar este atributo que tienen un significado especial:
      <variablelist>
	<varlistentry>
	  <term>TARGET="_blank"</term>
	  <listitem>
	    <para>Fuerza que el documento referenciado por el enlace
	      sea mostrado en una nueva ventana del navegador.</para>
	  </listitem>
	</varlistentry>
	<varlistentry>
	  <term>TARGET="_self"</term>
	  <listitem>
	    <para>
	      Usando este valor el documento enlazado será mostrado en
	      el mismo frame o ventana donde está el enlace. Este
	      valor es especialmente útil cuando se ha usado la
	      etiqueta <sgmltag>BASE</sgmltag> para especificar un
	      frame destino por defecto distinto del actual.
	    </para>
	  </listitem>
	</varlistentry>
	<varlistentry>
	  <term>TARGET="_parent"</term>
	  <listitem>
	    <para>
	      Este valor provoca que el documento sea mostrado en el
	      <sgmltag>FRAMESET</sgmltag> padre del frame actual.  
	    </para>
	  </listitem>
	</varlistentry>
	<varlistentry>
	  <term>TARGET="_top"</term>
	  <listitem>
	    <para>
	      Fuerza a que el enlace sea mostrado usando todo el
	      espacio de la ventana del navegador destruyendo toda
	      estructura deframes. Este valor debe ser usado siempre
	      que creemos un enlace a una página externa a nuestro
	      sitio web. 
	    </para>
	  </listitem>
	</varlistentry>
      </variablelist>
    </para>
    <para>
      En la figura 8.24 podemos ver una representación del significado
      de cada uno de estos valores que nos ayudará a resolver las
      dudas que tengamos sobre ellos. 
    </para>
    <figure id="fig8-24" label="8.24" xreflabel="figura 8.24">
      <title>
	Representación de los cuatro valores especiales del atributo
	<sgmltag class="attribute">TARGET</sgmltag>:
	<sgmltag class="attvalue">_self</sgmltag>,
	<sgmltag class="attvalue">_parent</sgmltag>,
	<sgmltag class="attvalue">_top</sgmltag>y
	<sgmltag class="attvalue">_blank</sgmltag>. 
      </title>
      <graphic fileref="img/fig8-24.png"/>
    </figure>
    <sect2>
      <title>Ejercicio con <sgmltag class="attribute">TARGET</sgmltag></title>
      <para>
	A continuación es aconsejable realizar algunas prácticas para
	habituarse al uso de estos valores. Cree una página sencilla de
	prueba:
	<programlisting format="linespecific">
	  &lt;HTML&gt;
	  &lt;HEAD&gt;&lt;/HEAD&gt;
	  &lt;BODY&gt; 
	  &lt;H1&gt;Página de prueba&lt;/H1&gt;
	  &lt;/BODY&gt;
	  &lt;/HTML&gt;
	</programlisting>
      </para>
      <para>
	Guárdela con el nombre
	<filename moreinfo="none">prueba.htm</filename>. A continuación
	usaremos el ejemplo de página con 3 frames creado
	anteriormente. A partir del archivo 
	<ulink
	       url="extra/ejem18/frame22.htm"><filename>extra/ejem18/frame22.htm></filename></ulink>
	edítelo, incluya el siguiente código en él y guárdelo en el
	mismo directorio que <filename>prueba.htm</filename>: 
	<programlisting format="linespecific">
	  &lt;A HREF="prueba.htm" TARGET="_self"&gt;
	  Enlace a la página de prueba
	  &lt;/A&gt;
	</programlisting>
      </para>
      <para>
	Para comprobar el efecto que se ha conseguido hay que abrir
	con el navegador la página <filename
					     moreinfo="none">index.htm</filename>, que era la que contenía
	los framesets y pulsar sobre el enlace. La página de prueba
	sustituirá al frame donde estaba el enlace.
      </para>
      <para>
	A continuación editamos de nuevo el archivo sustituyendo
	<sgmltag class="attribute">TARGET="_self"</sgmltag> por
	<sgmltag class="attribute">TARGET="_parent"</sgmltag>
	y volvemos a probar el enlace en el navegador. Seguidamente
	probamos los otros dos valores especiales. Este es un buen
	momento también para practicar usando los nombres de los
	frames como valor del atributo <sgmltag
						class="attribute">TARGET</sgmltag>.
      </para>
    </sect2>
  </sect1>
  <sect1>
    <title>Práctica 5: Web de Los Alpes con frames</title>
    <para>
      El objetivo de nuestra quinta práctica consistirá en realizar
      una versión con frames del sitio Web de la agencia de viajes Los
      Alpes que creamos en la práctica 4 (figura 8.25). Como es
      nuestro primer gran proyecto con frames nos limitaremos a la
      división de la ventana del navegador en dos frames fila evitando
      así las dificultades del anidamiento de frames. Nuestra
      recomendación para el lector avanzado es que realice la práctica
      tal y como aquí se expone y una vez completada con éxito intente
      mejorarla introduciendo un tercer frame. Este tercer frame puede
      servir por ejemplo para mantener siempre visible los botones de
      las páginas mes.htm y medida.htm. Para ello aconsejamos crear
      una división en dos columnas reservando la izquierda para los
      botones y usando la derecha para mostrar el contenido principal.
    </para>
    <figure float="0">
      <title>Página Web de Los Alpes creada en la práctica 4 de este curso. Ahora la modificaremos para crear una versión con frames.</title>
      <graphic fileref="img/fig8-25.png"/>
    </figure>
    <sect2>
      <title>Documento de definición de frames</title>
      <para>
	Volvamos a la versión de dos frames. El frame superior
	(banner) lo usaremos para mantener siempre visible el logo de
	Los Alpes junto con los botones que nos llevan a las distintas
	secciones de la página. En el frame inferior (principal)
	iremos mostrando cada una de las páginas de las que se compone
	el sitio Web. Estas deberán ser modificadas ya que el logo y
	los botones ya no deben mostrarse. En su momento veremos que
	cambios son necesarios. Por ahora empezaremos con el documento
	de definición de frames. Este será ahora el archivo inicial de
	nuestro sitio Web por lo que debe llamarse index.htm. El
	documento HTML con el contenido principal deberá ser
	renombrado. En esta ocasión hemos elegido darle el nombre
	<filename>main.htm</filename>. 
      </para>
      <para>
	Debemos dividir la ventana en dos filas. La fila superior debe
	tener una altura mínima de 163 pixeles para que quepan las
	imágenes que queremos incluir. La fila inferior ocupará el
	resto del espacio. El código que define esta estructura es por
	tanto: 
	<programlisting format="linespecific">
	  &lt;FRAMESET ROWS="163,*"&gt;
	  ... contenido frames ...
	  &lt;/FRAMESET&gt;
	</programlisting>
      </para>
      <para>
	Que nos divide la ventana en dos tal y como vemos en la figura
	8.26.
      </para>
      <figure float="0">
	<title>El frame superior será de 163 pixeles, mientras que el
	  inferior ocupará el resto del espacio de la ventana del navegador.</title>
	<graphic fileref="img/fig8-26.png"/>
      </figure>
      <para>
	El contenido del primero de los frames será el archivo
	banner.htm. El contenido del segundo será el archivo
	<filename>main.htm</filename>.
      </para>
      <para>
	Además de insertar el contenido de los frames debemos incluir
	un código alternativo para los navegadores que no soporten
	frames en el que mostraremos un texto acompañado de un enlace
	a una versión de las páginas sin frames. Para crear esta
	segunda versión creamos un directorio de nombre noframes y
	copiamos a él todos los documentos de la práctica 4. Con todo
	esto el código completo queda: 
	<programlisting format="linespecific">
	  &lt;HTML&gt;
	  &lt;HEAD&gt;
	  &lt;TITLE&gt;Agencia de viajes LOS ALPES&lt;/TITLE&gt;
	  &lt;/HEAD&gt;
	  &lt;FRAMESET ROWS="163,*"&gt;
	  &lt;FRAME SRC="banner.htm" NAME="banner" MARGINWIDHT="0"  
	  MARGINHEIGHT="0"&gt;
	  &lt;FRAME SRC="main.htm" NAME="principal"&gt;
	  &lt;/FRAMESET&gt;
	  &lt;NOFRAMES&gt;
	  &lt;BODY&gt;
	  Esta página tiene frames y su navegador no los
	  soporta. Por favor, visite nuestras páginas especialmente
	  preparadas para navegadores
	  &lt;A HREF="noframes/index.htm"&gt;sin frames&lt;/A&gt;.
	  &lt;/BODY&gt;
	  &lt;/NOFRAMES&gt;
	  &lt;/HTML&gt;
	</programlisting>
      </para>
      <para>
	Un aspecto que es necesario comentar de este código es que
	hemos usado los atributos <sgmltag
					   class="attribute">MARGINWIDTH</sgmltag> y 
	<sgmltag class="attribute">MARGINHEIGHT</sgmltag> dándoles el
	valor cero en el frame superior. Esta es una práctica muy
	habitual en los frames que contengan imágenes ya al eliminar
	los márgenes podremos reducir el tamaño del frame. 
      </para>
      <tip>
	<para>
	  En un capítulo anterior aprendimos a usar la etiqueta
	  <sgmltag>META</sgmltag>, para cargar automáticamente otra
	  página (o refrescar la misma) pasados unos segundos. Para
	  ello debíamos incluir en la cabecera del documento HTML el
	  siguiente código:
	  <programlisting format="linespecific">
	    &lt;META HTTP-EQUIV="refresh" CONTENT="5;URL=otrapagina.htm"&gt;
	  </programlisting>
	  Podemos usar esta técnica para redirigir a los usuarios de
	  navegadores sin soporte para frames a otras páginas que no
	  los tengan pasados 5 segundos.
	</para>
      </tip>
    </sect2>
    <sect2>
      <title>El frame Banner</title>
      <para>
	El contenido de este frame debe estar en un archivo que hemos
	decidido llamar banner.htm, y el siguiente paso en nuestro
	proyecto será escribir su código. Para ello no tenemos más que
	copiar las primeras líneas del archivo mes.htm (en realidad
	vale cualquiera menos main.htm). En estas primeras líneas se
	incluía el código para insertar el logo de Los Alpes y los
	botones que nos dirigían a las distintas secciones del sitio
	Web. Nuestro objetivo será que todo ello sea mostrado en este
	frame para que permanezca siempre visible y se muestre como en
	la figura 8.27 independientemente del contenido del frame
	inferior.
      </para>
      <figure float="0">
	<title>El contenido de banner.htm, el logo y encabezado de Los Alpes
	  así como los botones de navegación, serán mostrados en el frame
	  superior.</title>
	<graphic fileref="img/fig8-27.png"/>
      </figure>

      <para>
	Este es el momento de tener en cuenta un detalle importante de
	la creación de toda página con frames. En este documento hay
	una serie de enlaces (los botones) y debemos elegir cual será
	el destino donde se mostrará las páginas a las que nos
	conducen dichos enlaces. Si no especificamos ningún destino
	cuando pulsemos, por ejemplo, sobre el botón OFERTAS, el
	documento mes.htm sustituirá a <filename>banner.htm</filename>
	en el frame superior. Esto no es lo que queremos ya que
	precisamente el objetivo de este frame era mantenerse siempre
	visible. Queda claro por tanto que el mes.htm debe mostrarse
	en el frame principal. Ya sabemos como hacerlo, no hay más que
	usar el atributo <sgmltag class="attribute">TARGET</sgmltag>
	en la etiqueta que definía el enlace:
	<programlisting format="linespecific">
	  &lt;A HREF="mes.htm" TARGET="principal"&gt;
	  &lt;IMG SRC="img/boton2.jpg" WIDTH=136 HEIGHT=48
	  BORDER=0 ALT="Ofertas del Mes"&gt;
	  &lt;/A&gt;
	</programlisting>
      </para>
      <para>
	El siguiente paso sería añadir el atributo
	<sgmltag class="attribute">TARGET</sgmltag> en el resto de
	enlaces de banner.htm ya que todos ellos deben mostrar el
	resultado en el frame principal (figura 8.28). Sin embargo, si
	recordamos, en estos casos podíamos usar la etiqueta
	<sgmltag>BASE</sgmltag> en la cabecera del documento para
	indicar cual es el frame destino de todos los enlaces del
	documento. El código que debe emplearse es:
	<programlisting format="linespecific">
	  &lt;BASE TARGET=principal"&gt;
	</programlisting>
      </para>
      <figure float="0">
	<title>Todos los botones tienen como destino el frame principal. Por
	  tanto es conveniente usar la etiqueta <sgmltag>BASE</sgmltag> con el atributo
	  <sgmltag class="attribute">TARGET=principal</sgmltag>.</title>
	<graphic fileref="img/fig8-28.png"/>
      </figure>
      
      <para>
	Es importante recordar que la etiqueta <sgmltag>BASE</sgmltag>
	debía incluirse en la cabecera del documento, entre las
	instrucciones <sgmltag class="starttag">&lt;HEAD&gt;</sgmltag>
	y <sgmltag class="endtag">&lt;/HEAD&gt;</sgmltag>.  
      </para>
      <para>
	El código completo del archivo <filename>banner.htm</filename>
	es:
	<programlisting>
	  &lt;HTML&gt;

	  &lt;HEAD&gt;
	  &lt;TITLE&gt;LOS ALPES - Encabezado&lt;/TITLE&gt;
	  &lt;BASE TARGET="principal"&gt;
	  &lt;/HEAD&gt;

	  &lt;BODY LINK="#FED354" VLINK="#EDB301" ALINK="#FFFF00"
	  BGCOLOR="#335142" TEXT="#71AC84"
	  BACKGROUND="img/tratilea.jpg"&gt;
	  &lt;A NAME="arriba"&gt;&lt;/A&gt;
	  &lt;CENTER&gt;
	  &lt;TABLE ALIGN="center" border="0"&gt;
	  &lt;TR&gt;
	  &lt;TD&gt;&lt;IMG SRC="img/logo.gif" WIDTH=90 HEIGHT=99 BORDER=0
	  ALT="[LOGO]"&gt;&lt;/TD&gt;
	  &lt;TD&gt;&lt;IMG SRC="img/losalpes.gif" WIDTH=440
	  HEIGHT=99 BORDER=0 ALT="LOS ALPES - Agencia de
	  Viajes"&gt;&lt;/TD&gt; 
	  &lt;/TR&gt;
	  &lt;TR&gt;
	  &lt;TD COLSPAN="2"&gt;
	  &lt;A HREF="main.htm"&gt;&lt;IMG SRC="img/boton0.jpg"
	  WIDTH=110 HEIGHT=48 BORDER=0 ALT="Página
	  principal"&gt;&lt;/A&gt; 
	  &lt;A HREF="medida.htm"&gt;&lt;IMG SRC="img/boton1.jpg"
	  WIDTH=136 HEIGHT=48 BORDER=0 ALT="Viajes a su
	  medida"&gt;&lt;/A&gt; 
	  &lt;A HREF="mes.htm"&gt;&lt;IMG SRC="img/boton2.jpg"
	  WIDTH=136 HEIGHT=48 BORDER=0 ALT="Ofertas del Mes"&gt;&lt;/A&gt;
	  &lt;A HREF="superofe.htm"&gt;&lt;IMG SRC="img/boton3.jpg"
	  WIDTH=136 HEIGHT=48 BORDER=0 ALT="La
	  superOFERTA"&gt;&lt;/A&gt;&lt;/TD&gt; 
	  &lt;/TD&gt;
	  &lt;/TR&gt;
	  &lt;/TABLE&gt;
	  &lt;/CENTER&gt;
	  &lt;/BODY&gt;
	  &lt;/HTML&gt;
	</programlisting>
      </para>
    </sect2>
    <sect2>
      <title>Frame principal</title>
      <para>
	El frame inferior será nuestro frame principal, su contenido
	irá cambiando cuando el visitante navegue por nuestras
	páginas, mientras que el frame con el banner se mantendrá
	siempre igual. Este contenido será el de los documentos
	<filename>mes.htm</filename>,
	<filename>superofe.htm</filename>,
	<filename>medida.htm</filename> y
	<filename>main.htm</filename>. La manera de pasar de unos a
	otros será pulsar sobre los botones del frame banner. 
      </para>
      <para>
	Estas páginas ya existen y están creadas, sin embargo todas
	ellas contienen una cabecera con el título y la
	navegación. Estos elementos ya no son necesarios, dado que se
	encuentran en el frame superior. Por ello será necesario
	editar los archivos y quitar esa parte de cada uno. En la
	figura 8.29 se muestra el documento
	<filename>mes.htm</filename> una vez se le ha quitado la cabecera.
      </para>
      <figure float="0">
	<title>Página sin cabecera, lista para ser insertada en un frame.</title>
	<graphic fileref="img/fig8-29.png"/>
      </figure>
      
    </sect2>
    <sect2>
      <title>Últimos retoques</title>
      <para>
	El último paso en la creación del sitio Web de los Alpes en su
	versión con frames consistirá en dar los últimos
	retoques. Suele ser aconsejable asegurarnos que todo lo
	anterior funciona antes de realizarlos.
      </para>
      <para>
	Los retoques abarcan redimensionar los tamaños de los frames
	para adaptarlo al contenido final que se ha incluido en ellos,
	cambiar el fondo de algunos de ellos para que contrasten
	mejor, incluir márgenes para los párrafos, eliminar los
	bordes, etc. Nosotros nos centraremos en estas dos últimas
	modificaciones.
      </para>
      <para>
	Para eliminar el borde debemos editar de nuevo el archivo
	<filename>index.htm</filename> e incluir los atributos
	necesarios para hacer los bordes invisibles. En un principio,
	y para cumplir el estándar incluimos
	<sgmltag class="attribute">FRAMEBORDER="0"</sgmltag> en las
	dos etiquetas <sgmltag>FRAME</sgmltag> que tenía el
	documento. Posteriormente, para asegurar compatibilidad con
	todos los navegadores incluimos además: <sgmltag
							 class="attribute">FRAMEBORDER="0"</sgmltag>,
	<sgmltag class="attribute">BORDER="0"</sgmltag> y
	<sgmltag class="attribute">FRAMESPACING="0"</sgmltag> en la
	etiqueta <sgmltag>FRAMESET</sgmltag>. 
      </para>
      <para>
	Por otro lado editaremos el archivo main.htm que contenía el
	texto de bienvenida al sitio Web y daremos formato a dicho
	párrafo incluyendo márgenes a la izquierda y a la derecha
	suyo. Para crear los márgenes usaremos la técnica comentada en
	el capítulo dedicado a las tablas. Crearemos una de tres
	columnas. La izquierda estará vacía (contendrá un espacio
	vacío, &amp;nbsp;) y constituirá un margen con un ancho igual
	al 10% de la pantalla. La columna derecha, igualmente vacía,
	tendrá un ancho del 5% y constituye el margen derecho. En la
	columna central irá, por supuesto el texto, y ocupará el resto
	del espacio. 
      </para>
      <para>
	La práctica ha terminado. El resultado final se distribuye
	junto con este curso en un archivo comprimido:
	<ulink url="extra/practica5.tgz"><filename
						   moreinfo="none">practica5.tgz</filename></ulink>. 
	El resultado final se puede apreciar en la figura 8.30. 
      </para>
      <figure float="0">
	<title>Estructura de frames mostrando la página
	  <filename>main.htm</filename> en el frame central.</title>
	<graphic fileref="img/fig8-30.png"/>
      </figure>

    </sect2>
  </sect1>
  <sect1>
    <title>Frames flotantes</title>
    <para>
      Los frames flotantes son muy parecidos a los que hemos visto
      hasta ahora, 
      siendo su principal diferencia que pueden ser insertados en
      cualquier lugar de una página web, y su función ya no será
      dividir la ventana del navegador en varias partes, sino insertar
      en medio de un documento HTML el contenido de otro en un área
      del tamaño que indiquemos.
    </para>
    <note>
      <para>
	Los frames flotantes fueron originalmente una
	extensión de <productname class="trade">Internet
	  Explorer</productname> que posteriormente pasó a formar parte
	de el estándar HTML 4.0. Casi todos los
	navegadores usados actualmente lo soportan con la excepción de
	<productname class="trade">Netscape Navigator
	  4.x</productname>.
      </para>
    </note>

    <para>
      Para crear frames flotantes debemos usar la etiqueta
      <sgmltag>IFRAME</sgmltag> que significa <foreignphrase>Inline
	Frame</foreignphrase> por su similitud con las imágenes inline
      que se insertan en cualquier lugar de la página con la etiqueta
      <sgmltag>IMG</sgmltag>. La etiqueta <sgmltag>IFRAME</sgmltag> es
      un contenedor, esto es, está formada por una instrucción de
      inicio, <sgmltag class="starttag">&lt;IFRAME&gt;</sgmltag>,
      donde se especifican todos las características del frame por
      medio de atributos, y una instrucción de fin, <sgmltag
							     class="endtag">&lt;/IFRAME&gt;</sgmltag>. Entre estas dos
      instrucciones se puede incluir código alternativo para aquellos
      navegadores que no soporten los frames flotantes. Los que si lo
      hagan ignorarán dicho código. Dicho esto ya podemos escribir el
      código (de manera general) necesario para insertar un frame
      flotante:
      <programlisting format="linespecific">
	&lt;IFRAME ...&gt;
	...Código alternativo...
	&lt;/IFRAME&gt;
      </programlisting>
    </para>
    <para>
      Este código puede ser insertado en cualquier parte del cuerpo de
      una página. Esto quiere decir que para insertar un frame
      flotante no es necesaria ser una página especial para poder
      insertar frames. También es importante destacar que podemos
      insertar tantos frames flotantes como queramos.
    </para>
    <sect2>
      <title>Atributos de los frames flotantes</title>
      <para>
	En el código anterior incluimos tres puntos suspensivos en el
	interior de la instrucción &lt;FRAME&gt;, estos representan
	los atributos que debemos usar para indicar el documento HTML
	donde se encuentra el contenido que queremos insertar en el
	frame flotante, el tamaño de éste, el alineamiento y otras
	características variadas.
      </para>
      <para>
	El atributo <sgmltag class="attribute">SRC</sgmltag> es de uso
	obligatorio y toma como valor la dirección URL, absoluta o
	relativa, del documento HTML que servirá como
	contenido. Conocido este atributo ya podemos escribir un
	primer ejemplo de una página web con un frame flotante:
	<programlisting format="linespecific">
	  &lt;HTML&gt;
	  &lt;HEAD&gt;
	  &lt;TITLE&gt;Frames flotante&lt;/TITLE&gt;
	  &lt;/HEAD&gt;

	  &lt;BODY&gt;
	  &lt;H2&gt;A continuación vemos un ejemplo de un
	  frame flotante:&lt;/H2&gt;
	  &lt;IFRAME SRC="doc.htm"&gt;
	  Tu navegador no soporta frames flotantes, para ver
	  el contenido ve a &lt;A HREF="doc.htm"&gt;doc.htm&lt;/A&gt;
	  &lt;/IFRAME&gt;
	  &lt;/BODY&gt;

	  &lt;/HTML&gt;
	</programlisting>
      </para>
      <para>
	A continuación guardamos este archivo con el nombre
	<filename>iframe.htm</filename>, por ejemplo, y creamos el
	archivo doc.htm con un código HTML sencillo (nosotros hemos
	incluido un poco de texto, una imagen y un fondo azul) y
	visualizamos iframe.htm con Explorer obtendremos el resultado
	de la figura 8.31.
      </para>
      <figure float="0">
	<title>Los frames flotantes nos permiten insertar un documento HTML
	  completo en una página Web.</title>
	<graphic fileref="img/fig8-31.png"/>
      </figure>
      
      <para>
	Unas barras de desplazamiento delatan la presencia de un
	frame. Un aspecto a destacar de esta figura es que Explorer
	asigna un tamaño por defecto a dicho frame en el que mostrará
	todo el contenido del documento HTML que le hayamos indicado
	independientemente de lo largo que este sea.
      </para>
      <para>
	Si esta misma página es visualizada por un navegador sin
	soporte para frames flotantes, por ejemplo Netscape Navigator
	4.0, se mostraría el código que hemos introducido entre la
	instrucción de inicio y la de fin, como vemos en la figura
	8.32.
      </para>
      <figure float="0">
	<title>Los navegadores que no soporten frames flotantes mostrarán el
	  código alternativo incluido entre las instrucciones
	  <sgmltag class="starttag">&lt;IFRAME&gt;</sgmltag> y
	  <sgmltag class="endtag">&lt;/IFRAME&gt;</sgmltag></title>
	<graphic fileref="img/fig8-32.png"/>
      </figure>
      <para>
	Además del atributo <sgmltag class="attribute">SRC</sgmltag>
	la etiqueta <sgmltag class="attribute">IFRAME</sgmltag> tiene 
	los siguientes:
	<variablelist>
	  <varlistentry>
	    <term><sgmltag class="attribute">NAME</sgmltag></term>
	    <listitem>
	      <para>Este atributo nos permitirá asignarle un nombre al frame de
		manera que podamos referirnos a él por ejemplo para usarle como destino
		de un enlace usando el atributo <sgmltag class="attribute">TARGET</sgmltag>.</para> 
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">WIDTH</sgmltag> y <sgmltag class="attribute">HEIGTH</sgmltag></term>
	    <listitem>
	      <para>En la figura 8.30 hemos visto que Internet Explorer
		asignaba un tamaño por defecto al frame flotante. Usando estos dos
		atributos podremos indicar la anchura y altura respectivamente que no
		queramos. El valor dado será interpretado como un número en pixeles. Si
		en el código usado para obtener dicha figura añadimos un tamaño lo
		suficientemente grande desaparecerá la barra de desplazamiento.</para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">FRAMEBORDER</sgmltag></term>
	    <listitem>
	      <para>Este atributo funciona de igual manera que en los frames
		normales: sirve para quitar y poner los bordes. Escribiendo <sgmltag class="attribute">FRAMBORDER="0"</sgmltag>
		en el ejemplo realizado anteriormente obtenemos el resultado de la
		figura 8.33, en la que vemos que el borde del frame ha desaparecido. Sin
		embargo no hemos ampliado el tamaño del frame con los atributos <sgmltag class="attribute">WIDTH</sgmltag> y
		HEIGHT con lo que el documento es más grande que el espacio asignado y
		la barra de desplazamiento sigue ahí.</para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">SCROLLING</sgmltag></term>
	    <listitem>
	      <para>
		Este atributo nos permitirá quitar o mostrar la barra de
		desplazamiento. Puede tomar tres valores:
		<variablelist termlength="4">
		  <varlistentry>
		    <term><sgmltag class="attvalue">value</sgmltag></term>
		    <listitem>
		      <para>Es el valor por defecto e indica que la
			barra de desplazamiento sólo será mostrada si
			es necesario (es decir si el documento HTML
			completo no cabe en el espacio reservado al
			frame)</para> 
		    </listitem>
		  </varlistentry>
		  <varlistentry>
		    <term><sgmltag class="attvalue">yes</sgmltag></term>
		    <listitem>
		      <para>significa que las barras de desplazamiento
			deben mostrarse siempre</para>
		    </listitem>
		  </varlistentry>
		  <varlistentry>
		    <term><sgmltag class="attvalue">no</sgmltag></term>
		    <listitem>
		      <para>indica que no se mostrarán nunca.</para>
		    </listitem>
		  </varlistentry>
		</variablelist>
	      </para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">HSPACE</sgmltag> y
	      <sgmltag class="attribute">MARGINWIDTH</sgmltag></term> 
	    <listitem>
	      <para>
		Al igual que ocurría con las imágenes se puede
		especificar un margen alrededor del frame
		flotante. Con estos atributos concretos indicaremos la
		distancia mínima en pixeles que debe haber entre el
		frame y cualquier otro elemento que esté a su derecha
		o izquierda (margen horizontal). Es decir, sirve para
		especificar la anchura del margen. La única diferencia
		entre ambos atributos es que <sgmltag
						      class="attribute">MARGINWIDTH</sgmltag> es el nombre
		aceptado por el estándar y <sgmltag
						    class="attribute">HSPACE</sgmltag> está considerado
		como una extensión de Explorer. 
	      </para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">VSPACE</sgmltag> y <sgmltag class="attribute">MARGINHEIGHT</sgmltag></term>
	    <listitem>
	      <para>Con estos atributos indicaremos la altura en
		pixeles del margen del frame. Al igual que antes
		<sgmltag class="attribute">MARGINHEIGHT</sgmltag> es
		el nombre aceptado por el estándar y <sgmltag
							      class="attribute">VSPACE</sgmltag> está considerado
		como una extensión de Explorer. 
	      </para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">ALIGN</sgmltag></term>
	    <listitem>
	      <para>
		Permite alinear el frame respecto a la ventana del navegador y
		respecto al resto de elementos de la página. De nuevo, este atributo
		funciona exactamente igual que con las imágenes y puede tomar los mismos
		valores: left, middle, right, top y bottom. 
	      </para>
	      <para>En la figura 8.34 podemos ver un ejemplo con dos frames flotantes uno alineado a la izquierda (<sgmltag class="attribute">ALIGN="left"</sgmltag>) y el otro a la derecha (<sgmltag class="attribute">ALIGN="right"</sgmltag>). En el frame izquierdo hemos incluido un margen de 100 pixeles (<sgmltag class="attribute">MARGINWIDTH=100</sgmltag>) y en el derecho hemos quitado la barra de desplazamiento (<sgmltag class="attribute">SCROLLING=no</sgmltag>).</para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">BORDER</sgmltag></term>
	    <listitem>
	      <para>Este atributo es una extensión de Explorer y tiene la misma función que el atributo del mismo nombre de la etiqueta <sgmltag>FRAME</sgmltag>, indicar el grosor en pixeles del borde que delimita el frame.</para>
	    </listitem>
	  </varlistentry>
	  <varlistentry>
	    <term><sgmltag class="attribute">BORDERCOLOR</sgmltag></term>
	    <listitem>
	      <para>Con este atributo podremos cambiar el color del
		borde. Es, al igual que el anterior, una extensión de
		Explorer no aceptada por el estándar HTML 4.0.</para> 
	    </listitem>
	  </varlistentry>
	</variablelist>
      </para>

      <figure float="0">
	<title>Usando <sgmltag class="attribute">FRAMBORDER=0</sgmltag> podemos eliminar el borde del frame. Sin
	  embargo si el contenido no cabe en frame la barra de desplazamiento sí
	  se mostrará.
	</title>
	<graphic fileref="img/fig8-33.png"/>
      </figure>
      <figure float="0">
	<title>Con los atributos <sgmltag class="attribute">ALIGN</sgmltag>,
	  <sgmltag class="attribute">MARGINWIDTH</sgmltag> y
	  <sgmltag class="attribute">SCROLL</sgmltag> podremos
	  controlar las diversas características de los frames flotantes.</title>
	<graphic fileref="img/fig8-34.png"/>
      </figure>

      <para>
	Con esto terminamos el aprendizaje del uso de frames en
	páginas web. En el próximo capítulo del curso nos adentraremos
	en el mundo de los sonidos, vídeos y otros contenidos
	multimedia en el World Wide Web.
      </para>
    </sect2>
  </sect1>

</article>
