<?xml version="1.0" encoding="ISO-8859-1" ?>

<appendix id="smbldap-tools-smbldap_bind_conf">
	<title>Archivo de configuración <filename>/etc/smbldap-tools/smbldap_bind.conf</filename></title>

<programlisting><![CDATA[############################
# Credential Configuration #
############################
# Notes: you can specify two differents configuration if you use a
# master ldap for writing access and a slave ldap server for reading access
# By default, we will use the same DN (so it will work for standard Samba
# release)
slaveDN="cn=admin,dc=gsr,dc=pt"
slavePw="********"
masterDN="cn=admin,dc=gsr,dc=pt"
masterPw="********"]]></programlisting>
</appendix>

